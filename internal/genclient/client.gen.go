// Package genclient provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package genclient

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	"github.com/oapi-codegen/runtime"
)

// Defines values for CIStatus.
const (
	CIStatusFailure CIStatus = "failure"
	CIStatusNone    CIStatus = "none"
	CIStatusPending CIStatus = "pending"
	CIStatusSuccess CIStatus = "success"
)

// Valid indicates whether the value is a known member of the CIStatus enum.
func (e CIStatus) Valid() bool {
	switch e {
	case CIStatusFailure:
		return true
	case CIStatusNone:
		return true
	case CIStatusPending:
		return true
	case CIStatusSuccess:
		return true
	default:
		return false
	}
}

// Defines values for CheckState.
const (
	CheckStateCancelled CheckState = "cancelled"
	CheckStateFailure   CheckState = "failure"
	CheckStateQueued    CheckState = "queued"
	CheckStateRunning   CheckState = "running"
	CheckStateSkipped   CheckState = "skipped"
	CheckStateSuccess   CheckState = "success"
	CheckStateTimedOut  CheckState = "timed_out"
)

// Valid indicates whether the value is a known member of the CheckState enum.
func (e CheckState) Valid() bool {
	switch e {
	case CheckStateCancelled:
		return true
	case CheckStateFailure:
		return true
	case CheckStateQueued:
		return true
	case CheckStateRunning:
		return true
	case CheckStateSkipped:
		return true
	case CheckStateSuccess:
		return true
	case CheckStateTimedOut:
		return true
	default:
		return false
	}
}

// Defines values for Forge.
const (
	Forgejo Forge = "forgejo"
	Github  Forge = "github"
)

// Valid indicates whether the value is a known member of the Forge enum.
func (e Forge) Valid() bool {
	switch e {
	case Forgejo:
		return true
	case Github:
		return true
	default:
		return false
	}
}

// Defines values for ForgeErrorKind.
const (
	ForgeErrorKindConflict     ForgeErrorKind = "conflict"
	ForgeErrorKindNotFound     ForgeErrorKind = "not_found"
	ForgeErrorKindRateLimited  ForgeErrorKind = "rate_limited"
	ForgeErrorKindUnauthorized ForgeErrorKind = "unauthorized"
	ForgeErrorKindUnknown      ForgeErrorKind = "unknown"
	ForgeErrorKindUnreachable  ForgeErrorKind = "unreachable"
)

// Valid indicates whether the value is a known member of the ForgeErrorKind enum.
func (e ForgeErrorKind) Valid() bool {
	switch e {
	case ForgeErrorKindConflict:
		return true
	case ForgeErrorKindNotFound:
		return true
	case ForgeErrorKindRateLimited:
		return true
	case ForgeErrorKindUnauthorized:
		return true
	case ForgeErrorKindUnknown:
		return true
	case ForgeErrorKindUnreachable:
		return true
	default:
		return false
	}
}

// Defines values for HealthStatus.
const (
	Ok HealthStatus = "ok"
)

// Valid indicates whether the value is a known member of the HealthStatus enum.
func (e HealthStatus) Valid() bool {
	switch e {
	case Ok:
		return true
	default:
		return false
	}
}

// Defines values for MergeStatus.
const (
	MergeStatusBlocked     MergeStatus = "blocked"
	MergeStatusConflicting MergeStatus = "conflicting"
	MergeStatusMergeable   MergeStatus = "mergeable"
	MergeStatusUnknown     MergeStatus = "unknown"
)

// Valid indicates whether the value is a known member of the MergeStatus enum.
func (e MergeStatus) Valid() bool {
	switch e {
	case MergeStatusBlocked:
		return true
	case MergeStatusConflicting:
		return true
	case MergeStatusMergeable:
		return true
	case MergeStatusUnknown:
		return true
	default:
		return false
	}
}

// Defines values for PullRequestDependabotActionRequestAction.
const (
	Rebase   PullRequestDependabotActionRequestAction = "rebase"
	Recreate PullRequestDependabotActionRequestAction = "recreate"
)

// Valid indicates whether the value is a known member of the PullRequestDependabotActionRequestAction enum.
func (e PullRequestDependabotActionRequestAction) Valid() bool {
	switch e {
	case Rebase:
		return true
	case Recreate:
		return true
	default:
		return false
	}
}

// Defines values for SettingsResponseTheme.
const (
	SettingsResponseThemeDark  SettingsResponseTheme = "dark"
	SettingsResponseThemeEmpty SettingsResponseTheme = ""
	SettingsResponseThemeLight SettingsResponseTheme = "light"
)

// Valid indicates whether the value is a known member of the SettingsResponseTheme enum.
func (e SettingsResponseTheme) Valid() bool {
	switch e {
	case SettingsResponseThemeDark:
		return true
	case SettingsResponseThemeEmpty:
		return true
	case SettingsResponseThemeLight:
		return true
	default:
		return false
	}
}

// Defines values for ThemeRequestTheme.
const (
	ThemeRequestThemeDark  ThemeRequestTheme = "dark"
	ThemeRequestThemeEmpty ThemeRequestTheme = ""
	ThemeRequestThemeLight ThemeRequestTheme = "light"
)

// Valid indicates whether the value is a known member of the ThemeRequestTheme enum.
func (e ThemeRequestTheme) Valid() bool {
	switch e {
	case ThemeRequestThemeDark:
		return true
	case ThemeRequestThemeEmpty:
		return true
	case ThemeRequestThemeLight:
		return true
	default:
		return false
	}
}

// Defines values for ThemeResponseTheme.
const (
	ThemeResponseThemeDark  ThemeResponseTheme = "dark"
	ThemeResponseThemeEmpty ThemeResponseTheme = ""
	ThemeResponseThemeLight ThemeResponseTheme = "light"
)

// Valid indicates whether the value is a known member of the ThemeResponseTheme enum.
func (e ThemeResponseTheme) Valid() bool {
	switch e {
	case ThemeResponseThemeDark:
		return true
	case ThemeResponseThemeEmpty:
		return true
	case ThemeResponseThemeLight:
		return true
	default:
		return false
	}
}

// APIToken A personal API token's own metadata — never the token value
// itself, which only ever appears once, in
// APITokenCreateResponse's own response body at creation time.
type APIToken struct {
	CreatedAt time.Time `json:"createdAt"`

	// ExpiresAt When this token stops authenticating requests (#356) —
	// always set, mandatory at creation, no "never expires"
	// option. A token whose expiration has passed is rejected the
	// same way an invalid one is.
	ExpiresAt time.Time `json:"expiresAt"`
	Id        string    `json:"id"`

	// Label The name given at creation time, so a list of several is tellable apart.
	Label string `json:"label"`

	// LastUsedAt Omitted if this token has never authenticated a request yet.
	LastUsedAt *time.Time `json:"lastUsedAt,omitempty"`
}

// APITokenCreateRequest defines model for APITokenCreateRequest.
type APITokenCreateRequest struct {
	// ExpiresAt Must be in the future and no more than 366 days out (#356,
	// matching GitHub's own fine-grained-token maximum) — the
	// Settings UI offers 7/30/60/90-day presets (30 pre-selected)
	// or a custom date within that same cap, never an option for
	// no expiration at all.
	ExpiresAt time.Time `json:"expiresAt"`
	Label     string    `json:"label"`
}

// APITokenCreateResponse The one and only response that ever carries the raw token value
// — shown to the user once, at creation time; only its hash is
// stored, so it can't be recovered from here again. Losing it
// means generating a new one.
type APITokenCreateResponse struct {
	CreatedAt time.Time `json:"createdAt"`
	ExpiresAt time.Time `json:"expiresAt"`
	Id        string    `json:"id"`
	Label     string    `json:"label"`

	// Token The raw Bearer credential: `Authorization: Bearer <token>`.
	Token string `json:"token"`
}

// AdminUser defines model for AdminUser.
type AdminUser struct {
	CreatedAt   time.Time `json:"createdAt"`
	DisplayName string    `json:"displayName"`
	IsAdmin     bool      `json:"isAdmin"`
	Username    string    `json:"username"`
}

// BotPrUpdatesResponse See GET /api/settings/bot-pr-updates's own description.
type BotPrUpdatesResponse struct {
	AllowBotPrUpdates bool `json:"allowBotPrUpdates"`
}

// CIStatus The combined result across every check reported against the pull
// request's head commit. "none" means neither forge reported any
// check at all, not that one failed.
type CIStatus string

// Check One job/check run against a pull request's head commit.
type Check struct {
	Name string `json:"name"`

	// State One job/check's own status — the per-job detail CIStatus
	// deliberately doesn't carry, since CIStatus is the combined result
	// across every one of them.
	State CheckState `json:"state"`

	// Url That job's own page on the forge that ran it — never the pull request's own page.
	Url string `json:"url"`
}

// CheckState One job/check's own status — the per-job detail CIStatus
// deliberately doesn't carry, since CIStatus is the combined result
// across every one of them.
type CheckState string

// Credential A registered passkey's own metadata (#355) — never the
// credential itself, which never leaves the authenticator that
// created it; WebAuthn's whole design is that the server only ever
// sees a public key and signed assertions, not a secret to lose.
type Credential struct {
	CreatedAt time.Time `json:"createdAt"`

	// Id This passkey's own credential ID (base64url), distinct from the account's user ID.
	Id string `json:"id"`

	// Label The name given at registration time, so a list of several is tellable apart.
	Label string `json:"label"`
}

// Dashboard defines model for Dashboard.
type Dashboard struct {
	Forges []ForgeHealth `json:"forges"`

	// GeneratedAt When this snapshot was refreshed, not when it was requested.
	GeneratedAt  time.Time     `json:"generatedAt"`
	Issues       []Issue       `json:"issues"`
	PullRequests []PullRequest `json:"pullRequests"`
	Repos        []RepoStatus  `json:"repos"`
}

// Error defines model for Error.
type Error struct {
	Error string `json:"error"`
}

// FilterState The dashboard/Insights filter bar's own saved shape (#353) —
// whatever `filters.js`'s `loadState`/`saveState` already produce
// client-side (shared forge/repo/label/author/title/created/
// updated/groupBy, plus the two board-owned extras with no
// equivalent on the other entity type). This server stores and
// returns it verbatim, byte for byte, and never parses or
// validates its shape — the same "opaque blob, not this layer's
// concern" treatment WebAuthnCeremonyOptions above gets, so a
// client-side shape change here never needs a matching spec change.
type FilterState map[string]interface{}

// Forge defines model for Forge.
type Forge string

// ForgeErrorKind Why a forge is unreachable, or why a write to it was rejected, as
// coarse as every forge this service talks to can agree on.
// "unauthorized" covers both a bad token and a forbidden request;
// "not_found" usually means a wrong instance URL. "conflict" is only
// ever returned from a pull-request write — the forge reports the PR
// itself isn't currently mergeable. "unknown" covers a signal this
// service doesn't recognize, rather than a guessed-at specific cause.
type ForgeErrorKind string

// ForgeHealth defines model for ForgeHealth.
type ForgeHealth struct {
	// Error A human-readable explanation of the last failure, if reachable is false. Mapped from errorKind, not the raw underlying error text. Omitted when reachable.
	Error *string `json:"error,omitempty"`

	// ErrorKind Why a forge is unreachable, or why a write to it was rejected, as
	// coarse as every forge this service talks to can agree on.
	// "unauthorized" covers both a bad token and a forbidden request;
	// "not_found" usually means a wrong instance URL. "conflict" is only
	// ever returned from a pull-request write — the forge reports the PR
	// itself isn't currently mergeable. "unknown" covers a signal this
	// service doesn't recognize, rather than a guessed-at specific cause.
	ErrorKind *ForgeErrorKind `json:"errorKind,omitempty"`
	Forge     Forge           `json:"forge"`

	// RateLimitGraphQL GitHub's GraphQL budget (#361) — a completely separate 5000/hour allowance from rateLimitREST, populated from the rateLimit block GitHub embeds in every GraphQL response. Never set for a forge with no GraphQL API (Forgejo).
	RateLimitGraphQL *RateLimit `json:"rateLimitGraphQL,omitempty"`

	// RateLimitREST The forge's REST budget, from real X-RateLimit-* response headers. For GitHub, this is checkWebhooks and every write action (merge, update branch, comment, label) — a different 5000/hour allowance from rateLimitGraphQL, not a duplicate of it. Omitted on a poll that made no REST call at all (no webhook path configured). For Forgejo, or any forge reached through GenericSource, this is the only budget there is to report — GenericSource makes exclusively REST-style calls.
	RateLimitREST *RateLimit `json:"rateLimitREST,omitempty"`

	// Reachable Whether the last refresh attempt against this forge succeeded.
	Reachable bool `json:"reachable"`

	// RepoCount Repositories with write access this forge reported.
	RepoCount int `json:"repoCount"`
}

// Health defines model for Health.
type Health struct {
	Status HealthStatus `json:"status"`
}

// HealthStatus defines model for Health.Status.
type HealthStatus string

// Issue defines model for Issue.
type Issue struct {
	Author    string    `json:"author"`
	CreatedAt time.Time `json:"createdAt"`
	Forge     Forge     `json:"forge"`
	Labels    []Label   `json:"labels"`
	Number    int       `json:"number"`

	// Repo Examples: alrayyes/hush-hush
	Repo      string    `json:"repo"`
	Title     string    `json:"title"`
	UpdatedAt time.Time `json:"updatedAt"`

	// Url The real issue URL on its own forge.
	Url string `json:"url"`
}

// Label defines model for Label.
type Label struct {
	// Color The label's colour as a hex string with no leading "#",
	// exactly as the forge reports it.
	//
	//
	// Examples: d73a4a
	Color string `json:"color"`

	// Name Examples: kind/bug
	Name string `json:"name"`
}

// LoginBeginRequest defines model for LoginBeginRequest.
type LoginBeginRequest struct {
	// Username Examples: ryan
	Username string `json:"username"`
}

// MergeStatus A pull request's mergeable/blocked state, as coarse as every forge
// this service talks to can agree on. "blocked" covers anything
// stopping a merge that isn't confirmed to be a real conflict —
// including Forgejo's own mergeable flag reporting false, since its
// server computes that asynchronously and can report it stale.
// "unknown" covers both a forge that hasn't determined this yet and
// this service being unable to determine it.
type MergeStatus string

// PullRequest defines model for PullRequest.
type PullRequest struct {
	Author string `json:"author"`

	// AutoMergeEnabled Whether auto-merge is currently scheduled on this pull request.
	// Omitted when the owning forge has no way to report this at all
	// (Forgejo, today) — never false in that case, since this service
	// genuinely doesn't know.
	AutoMergeEnabled *bool `json:"autoMergeEnabled,omitempty"`

	// Behind Whether this pull request's base branch has moved since its
	// merge-base was computed. Deliberately its own field rather
	// than a mergeStatus value: on Forgejo a pull request can be
	// both mergeable and behind at once (its mergeable flag doesn't
	// get recomputed just because the base moved), so folding this
	// into mergeStatus would force picking one and losing the
	// other.
	Behind bool `json:"behind"`

	// Ci The combined result across every check reported against the pull
	// request's head commit. "none" means neither forge reported any
	// check at all, not that one failed.
	Ci        CIStatus  `json:"ci"`
	CreatedAt time.Time `json:"createdAt"`
	Draft     bool      `json:"draft"`
	Forge     Forge     `json:"forge"`
	Labels    []Label   `json:"labels"`

	// MergeStatus A pull request's mergeable/blocked state, as coarse as every forge
	// this service talks to can agree on. "blocked" covers anything
	// stopping a merge that isn't confirmed to be a real conflict —
	// including Forgejo's own mergeable flag reporting false, since its
	// server computes that asynchronously and can report it stale.
	// "unknown" covers both a forge that hasn't determined this yet and
	// this service being unable to determine it.
	MergeStatus MergeStatus `json:"mergeStatus"`
	Number      int         `json:"number"`

	// Repo Owner-qualified repository name.
	//
	// Examples: alrayyes/hush-hush
	Repo      string    `json:"repo"`
	Title     string    `json:"title"`
	UpdatedAt time.Time `json:"updatedAt"`

	// Url The real pull request URL on its own forge.
	Url string `json:"url"`
}

// PullRequestActionRequest Which pull request to act on.
type PullRequestActionRequest struct {
	Forge Forge `json:"forge"`

	// FullName "owner/repo", matching a Repo.fullName from GET /api/dashboard.
	FullName string `json:"fullName"`

	// Number Matching a PullRequest.number from GET /api/dashboard.
	Number int `json:"number"`
}

// PullRequestChecksResponse defines model for PullRequestChecksResponse.
type PullRequestChecksResponse struct {
	Checks []Check `json:"checks"`
}

// PullRequestDependabotActionRequest Which pull request to act on, and which of Dependabot's own comment commands to send.
type PullRequestDependabotActionRequest struct {
	// Action Which Dependabot comment command to post — "@dependabot rebase" or "@dependabot recreate".
	Action PullRequestDependabotActionRequestAction `json:"action"`
	Forge  Forge                                    `json:"forge"`

	// FullName "owner/repo", matching a Repo.fullName from GET /api/dashboard.
	FullName string `json:"fullName"`

	// Number Matching a PullRequest.number from GET /api/dashboard.
	Number int `json:"number"`
}

// PullRequestDependabotActionRequestAction Which Dependabot comment command to post — "@dependabot rebase" or "@dependabot recreate".
type PullRequestDependabotActionRequestAction string

// RateLimit One of a forge API's own request budgets for the credential the last refresh used — see ForgeHealth.rateLimitGraphQL/rateLimitREST for which budget this is and when each is (or isn't) reported.
type RateLimit struct {
	// Cost The point price the most recent call was actually charged (#440) — GraphQL-specific, since a REST request has no separate cost concept beyond the flat one-request-one-point REST's own budget already counts. Omitted for a REST-sourced RateLimit.
	Cost *int `json:"cost,omitempty"`

	// Limit Requests allowed per window.
	Limit int `json:"limit"`

	// Remaining Requests left in the current window.
	Remaining int `json:"remaining"`

	// ResetsAt When the window resets and remaining goes back to limit.
	ResetsAt time.Time `json:"resetsAt"`
}

// RegisterBeginRequest defines model for RegisterBeginRequest.
type RegisterBeginRequest struct {
	// DisplayName Examples: Ryan
	DisplayName string `json:"displayName"`

	// Username Examples: ryan
	Username string `json:"username"`
}

// RepoStatus One tracked repository, whether or not it currently has anything
// open — repoCount alone can't name it, and it wouldn't otherwise
// appear anywhere pullRequests/issues don't already mention it.
type RepoStatus struct {
	// AutoUpdateBranch Whether the signed-in user has turned on automatic branch
	// updates for this repo (#365) — any of its pull requests the
	// background refresh finds behind its base branch gets updated
	// the same way a manual "Update branch" click would, without
	// one. Suppressed for a bot-managed pull request (release-please,
	// Dependabot, Renovate) unless bot-PR updates are separately
	// allowed (POST /api/settings/bot-pr-updates), the same
	// restraint the manual button already applies.
	AutoUpdateBranch bool `json:"autoUpdateBranch"`

	// CanManageWebhooks Whether the signed-in user's own permission on this repo is
	// enough to list/create its webhooks — GitHub requires admin
	// specifically (write/maintain can push but still 404 on the
	// hooks endpoint, by GitHub's own design); Forgejo's equivalent
	// is its own admin permission flag. Always false for a repo
	// reached through an unauthenticated, username-only listing.
	// A point-in-time snapshot: a permission change since the last
	// refresh can still make a following ensure-webhook call fail
	// despite this having said true.
	CanManageWebhooks bool  `json:"canManageWebhooks"`
	Forge             Forge `json:"forge"`

	// FullName Examples: alrayyes/forge-dashboard
	FullName string `json:"fullName"`

	// HasWebhook Whether this app has ever recorded a signature-verified
	// webhook delivery for this repo. Passive: it reflects a real
	// delivery having arrived, not whether a webhook object exists
	// on the forge — a correctly configured webhook that just
	// hasn't fired yet still reads false here, the same as a
	// misconfigured one would.
	HasWebhook bool `json:"hasWebhook"`

	// Ignored Whether the signed-in user has ignored this repo (#363) — its
	// pullRequests/issues entries are excluded from this same
	// response and from Insights, but the repo itself still
	// appears here with accurate hasWebhook/canManageWebhooks.
	Ignored bool `json:"ignored"`

	// Url The repo's own page on its forge, for linking out.
	//
	// Examples: https://github.com/alrayyes/forge-dashboard
	Url string `json:"url"`
}

// SessionUser defines model for SessionUser.
type SessionUser struct {
	DisplayName string `json:"displayName"`
	IsAdmin     bool   `json:"isAdmin"`
	Username    string `json:"username"`
}

// SettingsRequest Replaces the signed-in user's saved GitHub/Forgejo configuration.
// A blank `githubToken` or `forgejoToken` keeps whatever token is
// already saved for that forge rather than clearing it — this is
// the only way to update the username fields without having to
// resubmit a token you don't want to re-paste. Theme isn't
// settable here at all — see PUT /api/settings/theme.
type SettingsRequest struct {
	AllowBotPrUpdates   *bool   `json:"allowBotPrUpdates,omitempty"`
	ForgejoToken        *string `json:"forgejoToken,omitempty"`
	ForgejoUrl          *string `json:"forgejoUrl,omitempty"`
	ForgejoUsername     *string `json:"forgejoUsername,omitempty"`
	GithubToken         *string `json:"githubToken,omitempty"`
	GithubUsername      *string `json:"githubUsername,omitempty"`
	RenovateRebaseLabel *string `json:"renovateRebaseLabel,omitempty"`
}

// SettingsResponse The signed-in user's own GitHub/Forgejo configuration. Never
// includes a forge token's value — only whether one is currently
// saved, so the UI can show a "Configured" badge without the secret
// ever reaching the browser. webhookToken/webhookSecret are the
// exception: they're ours to hand back in the clear, since the user
// has to paste them into the forge's own webhook setup.
type SettingsResponse struct {
	// AllowBotPrUpdates Overrides the default restraint the Update branch/Dependabot/
	// Renovate action buttons apply to a pull request opened by
	// release-please, Dependabot, or Renovate — those tools already
	// keep their own PRs current on their own schedule. False (the
	// default) leaves bot-managed PRs alone; true treats them the
	// same as any other PR.
	AllowBotPrUpdates bool `json:"allowBotPrUpdates"`

	// ForgejoTokenSet Whether a Forgejo token is currently saved.
	ForgejoTokenSet bool   `json:"forgejoTokenSet"`
	ForgejoUrl      string `json:"forgejoUrl"`

	// ForgejoUsername Used as a token-free public-repos fallback when no Forgejo token is set.
	ForgejoUsername string `json:"forgejoUsername"`

	// GithubTokenSet Whether a GitHub token is currently saved.
	GithubTokenSet bool `json:"githubTokenSet"`

	// GithubUsername Used as a token-free public-repos fallback when no GitHub token is set.
	GithubUsername string `json:"githubUsername"`

	// RenovateRebaseLabel The label Renovate's own rebase/retry trigger listens for on
	// a repo (Renovate's own `rebaseLabel` config option — genuinely
	// per-repo configurable, so this is a user-set override rather
	// than a hardcoded constant). Empty means Renovate's own
	// documented default, `rebase`.
	RenovateRebaseLabel string `json:"renovateRebaseLabel"`

	// Theme The signed-in user's own theme preference (#352). Empty
	// means "system" — follow the browser's prefers-color-scheme
	// rather than a saved choice. Set only from Settings; every
	// other page reads it via the lightweight
	// GET /api/settings/theme instead of this endpoint.
	Theme SettingsResponseTheme `json:"theme"`

	// WebhookSecret Pasted into the forge's own webhook "Secret" field, used to
	// verify the signature header on incoming webhook deliveries
	// (`X-Hub-Signature-256` on GitHub, `X-Forgejo-Signature` on
	// Forgejo). Never appears in the webhook URL itself.
	WebhookSecret string `json:"webhookSecret"`

	// WebhookToken Identifies this user in a webhook URL
	// (`/api/webhooks/{provider}/{webhookToken}`). Generated on
	// first visit to Settings and stable after that.
	WebhookToken string `json:"webhookToken"`
}

// SettingsResponseTheme The signed-in user's own theme preference (#352). Empty
// means "system" — follow the browser's prefers-color-scheme
// rather than a saved choice. Set only from Settings; every
// other page reads it via the lightweight
// GET /api/settings/theme instead of this endpoint.
type SettingsResponseTheme string

// SharedUser defines model for SharedUser.
type SharedUser struct {
	DisplayName string `json:"displayName"`
	Username    string `json:"username"`
}

// SharingResponse defines model for SharingResponse.
type SharingResponse struct {
	// SharedWith Users the signed-in user has shared their own dashboard with.
	SharedWith []SharedUser `json:"sharedWith"`

	// SharedWithMe Users who have shared their dashboard with the signed-in user.
	SharedWithMe []SharedUser `json:"sharedWithMe"`
}

// ThemeRequest See PUT /api/settings/theme's own description.
type ThemeRequest struct {
	Theme ThemeRequestTheme `json:"theme"`
}

// ThemeRequestTheme defines model for ThemeRequest.Theme.
type ThemeRequestTheme string

// ThemeResponse See GET /api/settings/theme's own description.
type ThemeResponse struct {
	Theme ThemeResponseTheme `json:"theme"`
}

// ThemeResponseTheme defines model for ThemeResponse.Theme.
type ThemeResponseTheme string

// Version defines model for Version.
type Version struct {
	// Version The release tag this binary was built from, with no leading
	// "v" — or "dev" for a build outside the release pipeline.
	//
	//
	// Examples: 0.12.1, dev
	Version string `json:"version"`
}

// WebAuthnCeremonyOptions The WebAuthn specification's own JSON shape for
// `PublicKeyCredentialCreationOptions`/`PublicKeyCredentialRequestOptions`
// (the begin responses) and `PublicKeyCredential`
// (the finish request bodies) — passed through to and from the
// browser's own `navigator.credentials` API untouched, so this
// contract intentionally doesn't re-model WebAuthn's own schema.
type WebAuthnCeremonyOptions map[string]interface{}

// WebhookEnsureRequest Which repo to create or fix up a webhook on.
type WebhookEnsureRequest struct {
	Forge Forge `json:"forge"`

	// FullName "owner/repo", matching a Repo.fullName from GET /api/dashboard.
	FullName string `json:"fullName"`
}

// PathTokenID defines model for PathTokenID.
type PathTokenID = string

// PathUsername defines model for PathUsername.
type PathUsername = string

// PathWebhookToken defines model for PathWebhookToken.
type PathWebhookToken = string

// FinishAddCredentialParams defines parameters for FinishAddCredential.
type FinishAddCredentialParams struct {
	// Label A name the user recognizes, e.g. "MacBook" or "YubiKey"
	// (#355) — prompted for at registration time, never derived
	// from the authenticator itself.
	Label string `form:"label" json:"label"`
}

// FinishLoginParams defines parameters for FinishLogin.
type FinishLoginParams struct {
	Username string `form:"username" json:"username"`
}

// FinishRegistrationParams defines parameters for FinishRegistration.
type FinishRegistrationParams struct {
	Username string `form:"username" json:"username"`
}

// GetDashboardParams defines parameters for GetDashboard.
type GetDashboardParams struct {
	// Owner A username that has shared their dashboard with the caller. Defaults to the caller's own.
	Owner *string `form:"owner,omitempty" json:"owner,omitempty"`
}

// GetPullRequestChecksParams defines parameters for GetPullRequestChecks.
type GetPullRequestChecksParams struct {
	Forge Forge `form:"forge" json:"forge"`

	// FullName "owner/repo", matching a Repo.fullName from GET /api/dashboard.
	FullName string `form:"fullName" json:"fullName"`

	// Number Matching a PullRequest.number from GET /api/dashboard.
	Number int `form:"number" json:"number"`
}

// ReceiveForgejoWebhookJSONBody defines parameters for ReceiveForgejoWebhook.
type ReceiveForgejoWebhookJSONBody = map[string]interface{}

// ReceiveGitHubWebhookJSONBody defines parameters for ReceiveGitHubWebhook.
type ReceiveGitHubWebhookJSONBody = map[string]interface{}

// FinishAddCredentialJSONRequestBody defines body for FinishAddCredential for application/json ContentType.
type FinishAddCredentialJSONRequestBody = WebAuthnCeremonyOptions

// BeginLoginJSONRequestBody defines body for BeginLogin for application/json ContentType.
type BeginLoginJSONRequestBody = LoginBeginRequest

// FinishLoginJSONRequestBody defines body for FinishLogin for application/json ContentType.
type FinishLoginJSONRequestBody = WebAuthnCeremonyOptions

// BeginRegistrationJSONRequestBody defines body for BeginRegistration for application/json ContentType.
type BeginRegistrationJSONRequestBody = RegisterBeginRequest

// FinishRegistrationJSONRequestBody defines body for FinishRegistration for application/json ContentType.
type FinishRegistrationJSONRequestBody = WebAuthnCeremonyOptions

// PostPullRequestDependabotActionJSONRequestBody defines body for PostPullRequestDependabotAction for application/json ContentType.
type PostPullRequestDependabotActionJSONRequestBody = PullRequestDependabotActionRequest

// MergePullRequestJSONRequestBody defines body for MergePullRequest for application/json ContentType.
type MergePullRequestJSONRequestBody = PullRequestActionRequest

// PostPullRequestRenovateRebaseJSONRequestBody defines body for PostPullRequestRenovateRebase for application/json ContentType.
type PostPullRequestRenovateRebaseJSONRequestBody = PullRequestActionRequest

// UpdatePullRequestBranchJSONRequestBody defines body for UpdatePullRequestBranch for application/json ContentType.
type UpdatePullRequestBranchJSONRequestBody = PullRequestActionRequest

// DisableAutoUpdateBranchJSONRequestBody defines body for DisableAutoUpdateBranch for application/json ContentType.
type DisableAutoUpdateBranchJSONRequestBody = WebhookEnsureRequest

// EnableAutoUpdateBranchJSONRequestBody defines body for EnableAutoUpdateBranch for application/json ContentType.
type EnableAutoUpdateBranchJSONRequestBody = WebhookEnsureRequest

// IgnoreRepoJSONRequestBody defines body for IgnoreRepo for application/json ContentType.
type IgnoreRepoJSONRequestBody = WebhookEnsureRequest

// UnignoreRepoJSONRequestBody defines body for UnignoreRepo for application/json ContentType.
type UnignoreRepoJSONRequestBody = WebhookEnsureRequest

// PutSettingsJSONRequestBody defines body for PutSettings for application/json ContentType.
type PutSettingsJSONRequestBody = SettingsRequest

// SetFilterStateJSONRequestBody defines body for SetFilterState for application/json ContentType.
type SetFilterStateJSONRequestBody = FilterState

// SetThemeJSONRequestBody defines body for SetTheme for application/json ContentType.
type SetThemeJSONRequestBody = ThemeRequest

// CreateAPITokenJSONRequestBody defines body for CreateAPIToken for application/json ContentType.
type CreateAPITokenJSONRequestBody = APITokenCreateRequest

// EnsureWebhookJSONRequestBody defines body for EnsureWebhook for application/json ContentType.
type EnsureWebhookJSONRequestBody = WebhookEnsureRequest

// ReceiveForgejoWebhookJSONRequestBody defines body for ReceiveForgejoWebhook for application/json ContentType.
type ReceiveForgejoWebhookJSONRequestBody = ReceiveForgejoWebhookJSONBody

// ReceiveGitHubWebhookJSONRequestBody defines body for ReceiveGitHubWebhook for application/json ContentType.
type ReceiveGitHubWebhookJSONRequestBody = ReceiveGitHubWebhookJSONBody

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// ListUsers List every registered user
	//
	// Never includes a passkey or a forge credential — a username,
	// display name, admin flag and registration timestamp per user.
	//
	// Corresponds with GET /api/admin/users (the `ListUsers` operationId).
	ListUsers(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteUser Remove a user outright
	//
	// Deletes the account, every session, every registered passkey, and
	// every saved forge credential — irreversible. Their username
	// becomes available for a fresh registration.
	//
	// Corresponds with DELETE /api/admin/users/{username} (the `DeleteUser` operationId).
	DeleteUser(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RevokeUser Revoke a user's passkeys and sessions
	//
	// Deletes every session and every registered passkey for this user —
	// they're signed out everywhere immediately, and have to register a
	// new passkey from scratch to get back in. Their saved forge
	// credentials are untouched; use the delete endpoint to remove the
	// account itself.
	//
	// Corresponds with POST /api/admin/users/{username}/revoke (the `RevokeUser` operationId).
	RevokeUser(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListCredentials List the signed-in user's own passkeys
	//
	// Every passkey on the account (#355), oldest first — not just the
	// one used to establish the current session.
	//
	// Corresponds with GET /api/auth/credentials (the `ListCredentials` operationId).
	ListCredentials(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BeginAddCredential Start a ceremony to add another passkey to the signed-in account
	//
	// The authenticated counterpart to POST /api/auth/register/begin —
	// that one only ever works for a brand-new, credential-less
	// account; this is how an already-registered user adds a second
	// (or third...) passkey, e.g. a laptop and a security key (#355).
	//
	// Corresponds with POST /api/auth/credentials/begin (the `BeginAddCredential` operationId).
	BeginAddCredential(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishAddCredentialWithBody Complete an add-credential ceremony
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
	FinishAddCredentialWithBody(ctx context.Context, params *FinishAddCredentialParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishAddCredential Complete an add-credential ceremony
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
	FinishAddCredential(ctx context.Context, params *FinishAddCredentialParams, body FinishAddCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteCredential Remove one of the signed-in user's own passkeys
	//
	// Refused on the account's last remaining passkey (#355) — this
	// app is WebAuthn-only with no password fallback, so deleting it
	// would lock the account out entirely.
	//
	// Corresponds with DELETE /api/auth/credentials/{id} (the `DeleteCredential` operationId).
	DeleteCredential(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BeginLoginWithBody Start a passkey login ceremony
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
	BeginLoginWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BeginLogin Start a passkey login ceremony
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
	BeginLogin(ctx context.Context, body BeginLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishLoginWithBody Complete a passkey login ceremony
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
	FinishLoginWithBody(ctx context.Context, params *FinishLoginParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishLogin Complete a passkey login ceremony
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
	FinishLogin(ctx context.Context, params *FinishLoginParams, body FinishLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Logout End the current session
	//
	// Corresponds with POST /api/auth/logout (the `Logout` operationId).
	Logout(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BeginRegistrationWithBody Start a passkey registration ceremony
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
	BeginRegistrationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BeginRegistration Start a passkey registration ceremony
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
	BeginRegistration(ctx context.Context, body BeginRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishRegistrationWithBody Complete a passkey registration ceremony
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
	FinishRegistrationWithBody(ctx context.Context, params *FinishRegistrationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// FinishRegistration Complete a passkey registration ceremony
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
	FinishRegistration(ctx context.Context, params *FinishRegistrationParams, body FinishRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSession Who, if anyone, the current session cookie belongs to
	//
	// Corresponds with GET /api/auth/session (the `GetSession` operationId).
	GetSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetDashboard The aggregated view
	//
	// Returns the most recently fetched snapshot. The backend refreshes
	// this on its own schedule in the background; this endpoint never
	// blocks on a live call to either forge, so it answers instantly even
	// when a forge is slow or unreachable.
	//
	// Omitting `owner` returns the signed-in user's own dashboard. Passing
	// it returns that user's dashboard instead — allowed only when they've
	// shared it with the caller (or the caller is viewing their own
	// username), and refused with a 403 otherwise.
	//
	// Corresponds with GET /api/dashboard (the `GetDashboard` operationId).
	GetDashboard(ctx context.Context, params *GetDashboardParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RefreshDashboard Trigger an immediate refresh of the signed-in user's own dashboard
	//
	// Blocks until an immediate, out-of-band refresh of the signed-in
	// user's own dashboard completes, then returns the resulting
	// snapshot — for retrying right away after a forge was only
	// briefly unreachable, instead of waiting for the next scheduled
	// background refresh.
	//
	// Doesn't support `owner` the way GET /api/dashboard does — only
	// ever the signed-in user's own dashboard, never a shared one, so
	// a user with shared access to someone else's dashboard can never
	// spend that owner's own forge rate-limit budget on their own
	// schedule.
	//
	// Corresponds with POST /api/dashboard/refresh (the `RefreshDashboard` operationId).
	RefreshDashboard(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// StreamDashboard Server-Sent Events stream of the signed-in user's own dashboard
	//
	// Pushes a fresh snapshot every time this user's background
	// refresh produces one — most notably right after a verified
	// webhook delivery (see the webhooks tag) triggers an immediate
	// one, which is what turns that into a live update instead of
	// something only the next scheduled refresh picks up.
	//
	// Doesn't support `owner` the way GET /api/dashboard does — only
	// ever the signed-in user's own dashboard, never a shared one.
	// The frontend's own 30-second poll against GET /api/dashboard
	// keeps running unconditionally, connected or not — a browser or
	// proxy that can't hold an SSE connection open just never
	// benefits from this, rather than the dashboard going stale
	// silently.
	//
	// Corresponds with GET /api/dashboard/stream (the `StreamDashboard` operationId).
	StreamDashboard(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetPullRequestChecks List every job/check run against a pull request's head commit, on demand
	//
	// Fetched live on every call, never as part of GET /api/dashboard's
	// own snapshot — this is detail a pull request's row doesn't show
	// until a caller actually asks for it, and adding it to the eager
	// refresh loop would multiply that refresh's cost by every open
	// pull request for data most of them are never opened for.
	//
	// Falls back to the legacy combined commit-status API's own
	// per-check entries when the forge reports no Actions runs at all
	// for that commit (Actions disabled, or an instance too old to
	// expose them) — never an error for that case specifically, since
	// it's a normal, expected shape for an older or CI-less repo.
	//
	// Corresponds with GET /api/pull-requests/checks (the `GetPullRequestChecks` operationId).
	GetPullRequestChecks(ctx context.Context, params *GetPullRequestChecksParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PostPullRequestDependabotActionWithBody Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
	//
	// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
	// comment on the named pull request — Dependabot's own documented
	// comment-command interface, not a generic "post any comment"
	// endpoint; `action` is validated server-side to one of the two
	// values below and nothing else is ever sent. GitHub only —
	// Dependabot doesn't run on Forgejo.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
	PostPullRequestDependabotActionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PostPullRequestDependabotAction Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
	//
	// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
	// comment on the named pull request — Dependabot's own documented
	// comment-command interface, not a generic "post any comment"
	// endpoint; `action` is validated server-side to one of the two
	// values below and nothing else is ever sent. GitHub only —
	// Dependabot doesn't run on Forgejo.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
	PostPullRequestDependabotAction(ctx context.Context, body PostPullRequestDependabotActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MergePullRequestWithBody Merge one pull request, on the signed-in user's behalf
	//
	// Merges the named pull request using its repo's own configured
	// default merge method — GitHub is asked to pick its own repo
	// default; Forgejo's API has no such default built in, so this
	// looks up the repo's configured default merge style first and
	// passes that explicitly. Neither takes a method override here;
	// picking one is out of scope for this endpoint.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
	MergePullRequestWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MergePullRequest Merge one pull request, on the signed-in user's behalf
	//
	// Merges the named pull request using its repo's own configured
	// default merge method — GitHub is asked to pick its own repo
	// default; Forgejo's API has no such default built in, so this
	// looks up the repo's configured default merge style first and
	// passes that explicitly. Neither takes a method override here;
	// picking one is out of scope for this endpoint.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
	MergePullRequest(ctx context.Context, body MergePullRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PostPullRequestRenovateRebaseWithBody Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
	//
	// Adds the signed-in user's own configured Renovate rebase label
	// (Settings' "Renovate rebase label" field, defaulting to
	// Renovate's own `rebase` if left blank) to the named pull
	// request — Renovate's own documented rebase/retry trigger, which
	// is a label rather than a comment command. Works on both GitHub
	// and Forgejo, unlike the Dependabot actions, since Renovate runs
	// on both.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
	PostPullRequestRenovateRebaseWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PostPullRequestRenovateRebase Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
	//
	// Adds the signed-in user's own configured Renovate rebase label
	// (Settings' "Renovate rebase label" field, defaulting to
	// Renovate's own `rebase` if left blank) to the named pull
	// request — Renovate's own documented rebase/retry trigger, which
	// is a label rather than a comment command. Works on both GitHub
	// and Forgejo, unlike the Dependabot actions, since Renovate runs
	// on both.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
	PostPullRequestRenovateRebase(ctx context.Context, body PostPullRequestRenovateRebaseJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePullRequestBranchWithBody Bring one pull request's branch up to date with its base, on the signed-in user's behalf
	//
	// Merges the named pull request's base branch into its own head
	// branch. GitHub can schedule this as a background job rather than
	// finishing it inline — a 202 here means exactly that, not a
	// failure; a follow-up refresh a moment later reflects the result.
	// Forgejo's equivalent is always synchronous, so it only ever
	// answers 204.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
	UpdatePullRequestBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePullRequestBranch Bring one pull request's branch up to date with its base, on the signed-in user's behalf
	//
	// Merges the named pull request's base branch into its own head
	// branch. GitHub can schedule this as a background job rather than
	// finishing it inline — a 202 here means exactly that, not a
	// failure; a follow-up refresh a moment later reflects the result.
	// Forgejo's equivalent is always synchronous, so it only ever
	// answers 204.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
	UpdatePullRequestBranch(ctx context.Context, body UpdatePullRequestBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DisableAutoUpdateBranchWithBody Turn off automatic branch updates for one tracked repo
	//
	// The reverse of POST /api/repos/auto-update-branch/enable.
	// Idempotent: disabling a repo that was never enabled is a no-op,
	// not an error.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
	DisableAutoUpdateBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DisableAutoUpdateBranch Turn off automatic branch updates for one tracked repo
	//
	// The reverse of POST /api/repos/auto-update-branch/enable.
	// Idempotent: disabling a repo that was never enabled is a no-op,
	// not an error.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
	DisableAutoUpdateBranch(ctx context.Context, body DisableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EnableAutoUpdateBranchWithBody Turn on automatic branch updates for one tracked repo
	//
	// Any of this repo's pull requests the background refresh finds
	// behind its base branch gets updated automatically from then on
	// (#365), the same as clicking "Update branch" would — suppressed
	// for a bot-managed pull request unless bot-PR updates are
	// separately allowed. Idempotent: enabling an already-enabled repo
	// is a no-op, not an error.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
	EnableAutoUpdateBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EnableAutoUpdateBranch Turn on automatic branch updates for one tracked repo
	//
	// Any of this repo's pull requests the background refresh finds
	// behind its base branch gets updated automatically from then on
	// (#365), the same as clicking "Update branch" would — suppressed
	// for a bot-managed pull request unless bot-PR updates are
	// separately allowed. Idempotent: enabling an already-enabled repo
	// is a no-op, not an error.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
	EnableAutoUpdateBranch(ctx context.Context, body EnableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// IgnoreRepoWithBody Hide one tracked repo's pull requests and issues from the dashboard and Insights
	//
	// Reversible, not destructive (#363): the repo itself keeps
	// appearing in GET /api/dashboard's `repos` array with accurate
	// webhook-coverage status, and keeps being fetched and counted —
	// only its pullRequests/issues entries stop appearing there and on
	// Insights. Idempotent: ignoring an already-ignored repo is a
	// no-op, not an error.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
	IgnoreRepoWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// IgnoreRepo Hide one tracked repo's pull requests and issues from the dashboard and Insights
	//
	// Reversible, not destructive (#363): the repo itself keeps
	// appearing in GET /api/dashboard's `repos` array with accurate
	// webhook-coverage status, and keeps being fetched and counted —
	// only its pullRequests/issues entries stop appearing there and on
	// Insights. Idempotent: ignoring an already-ignored repo is a
	// no-op, not an error.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
	IgnoreRepo(ctx context.Context, body IgnoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UnignoreRepoWithBody Stop ignoring one tracked repo
	//
	// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
	// repo that was never ignored is a no-op, not an error.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
	UnignoreRepoWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UnignoreRepo Stop ignoring one tracked repo
	//
	// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
	// repo that was never ignored is a no-op, not an error.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
	UnignoreRepo(ctx context.Context, body UnignoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSettings The signed-in user's own forge configuration
	//
	// Tokens are never returned, before or after being saved — only
	// whether one is currently on file. There is no way to read a saved
	// token back out through this API.
	//
	// Corresponds with GET /api/settings (the `GetSettings` operationId).
	GetSettings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PutSettingsWithBody Save the signed-in user's forge configuration
	//
	// Replaces every field except a blank token, which means "keep
	// whatever's already saved" rather than "clear it" — the only
	// sensible behavior for a field the browser is never shown the
	// current value of. Saving triggers that user's dashboard to start
	// refreshing from the new configuration.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /api/settings (the `PutSettings` operationId).
	PutSettingsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PutSettings Save the signed-in user's forge configuration
	//
	// Replaces every field except a blank token, which means "keep
	// whatever's already saved" rather than "clear it" — the only
	// sensible behavior for a field the browser is never shown the
	// current value of. Saving triggers that user's dashboard to start
	// refreshing from the new configuration.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /api/settings (the `PutSettings` operationId).
	PutSettings(ctx context.Context, body PutSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBotPrUpdatesSetting Whether bot-managed pull request branches can be updated
	//
	// A lightweight, side-effect-free read of one field —
	// allowBotPrUpdates — for the dashboard page to check on every
	// load. Deliberately not GET /api/settings itself: that handler
	// also provisions webhook credentials on first call
	// (EnsureWebhookCredentials), which the dashboard visiting on a
	// user's behalf shouldn't trigger for someone who's never opened
	// Settings at all.
	//
	// Corresponds with GET /api/settings/bot-pr-updates (the `GetBotPrUpdatesSetting` operationId).
	GetBotPrUpdatesSetting(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetFilterState The signed-in user's own saved dashboard/Insights filter state
	//
	// A lightweight read of one opaque blob (#353), the same
	// "deliberately not GET /api/settings itself" restraint
	// GET /api/settings/bot-pr-updates already uses, for the same
	// reason: this loads on every dashboard/Insights visit and
	// shouldn't provision webhook credentials as a side effect.
	// `{}` for a user who's never saved any filters yet, not a 404.
	//
	// Corresponds with GET /api/settings/filter-state (the `GetFilterState` operationId).
	GetFilterState(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetFilterStateWithBody Save the signed-in user's own dashboard/Insights filter state
	//
	// A dedicated, lightweight save separate from the main
	// PUT /api/settings — filters change on nearly every click, a
	// mismatch for that endpoint's "always a full form submit"
	// convention (#353). The body replaces the saved state entirely,
	// the same "always a full submit, just of a much smaller and
	// much more frequent thing" shape as the main settings PUT, not a
	// partial patch. Filters.js's own client-side code decides when
	// to call this: immediately for a discrete control, debounced
	// while the user is still typing in the free-text Title filter.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
	SetFilterStateWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetFilterState Save the signed-in user's own dashboard/Insights filter state
	//
	// A dedicated, lightweight save separate from the main
	// PUT /api/settings — filters change on nearly every click, a
	// mismatch for that endpoint's "always a full form submit"
	// convention (#353). The body replaces the saved state entirely,
	// the same "always a full submit, just of a much smaller and
	// much more frequent thing" shape as the main settings PUT, not a
	// partial patch. Filters.js's own client-side code decides when
	// to call this: immediately for a discrete control, debounced
	// while the user is still typing in the free-text Title filter.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
	SetFilterState(ctx context.Context, body SetFilterStateJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetTheme The signed-in user's own saved theme preference
	//
	// A lightweight, side-effect-free read of one field — theme — for
	// every page to check on load (#352). The same "deliberately not
	// GET /api/settings itself" restraint GET /api/settings/bot-pr-updates
	// already uses, for the same reason: this loads on every page visit
	// and shouldn't provision webhook credentials as a side effect.
	//
	// Corresponds with GET /api/settings/theme (the `GetTheme` operationId).
	GetTheme(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetThemeWithBody Save the signed-in user's own theme preference
	//
	// A dedicated, instant save (#352) — deliberately not routed
	// through the main PUT /api/settings, whose every other field is
	// a plain replace rather than a per-field merge: a request
	// carrying only theme through that handler would blank every
	// other saved setting. Picking a theme applies immediately, the
	// same "set once and forget" convention the ticket's own research
	// cites, rather than waiting on the rest of the settings form's
	// own Save button.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
	SetThemeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetTheme Save the signed-in user's own theme preference
	//
	// A dedicated, instant save (#352) — deliberately not routed
	// through the main PUT /api/settings, whose every other field is
	// a plain replace rather than a per-field merge: a request
	// carrying only theme through that handler would blank every
	// other saved setting. Picking a theme applies immediately, the
	// same "set once and forget" convention the ticket's own research
	// cites, rather than waiting on the rest of the settings form's
	// own Save button.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
	SetTheme(ctx context.Context, body SetThemeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSharing The signed-in user's sharing relationships
	//
	// Both directions at once: who the signed-in user has shared their
	// own dashboard with, and whose dashboards have been shared with
	// them.
	//
	// Corresponds with GET /api/sharing (the `GetSharing` operationId).
	GetSharing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UnshareWith Stop sharing the signed-in user's dashboard with username
	//
	// Idempotent — removing a username that was never shared with changes nothing.
	//
	// Corresponds with DELETE /api/sharing/{username} (the `UnshareWith` operationId).
	UnshareWith(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ShareWith Share the signed-in user's dashboard with username
	//
	// Idempotent — sharing with a username already on the list changes
	// nothing. username needs no tokens of their own configured to view
	// the shared dashboard; they see the owner's aggregated data exactly
	// as the owner sees it.
	//
	// Corresponds with PUT /api/sharing/{username} (the `ShareWith` operationId).
	ShareWith(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAPITokens List the signed-in user's own personal API tokens
	//
	// Metadata only, per APIToken's own description — a raw value never reappears once created.
	//
	// Corresponds with GET /api/tokens (the `ListAPITokens` operationId).
	ListAPITokens(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAPITokenWithBody Generate a new personal API token
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
	CreateAPITokenWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAPIToken Generate a new personal API token
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
	CreateAPIToken(ctx context.Context, body CreateAPITokenJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteAPIToken Revoke a personal API token
	//
	// Stops the token authenticating immediately. Idempotent — deleting
	// an id that's already gone (or never belonged to the signed-in
	// user) still answers 204, the same as any other delete-by-id
	// endpoint here (compare DELETE /api/sharing/{username}), so a
	// double-click or a retried request isn't an error.
	//
	// Corresponds with DELETE /api/tokens/{id} (the `DeleteAPIToken` operationId).
	DeleteAPIToken(ctx context.Context, id PathTokenID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetVersion The running server's own version
	//
	// The tag goreleaser stamped the running binary with ("dev" for a
	// build outside the release pipeline). Public and unauthenticated —
	// every page, including the pre-login screen, links to the matching
	// GitHub release from its footer so a person can tell what's
	// actually deployed without shelling into the box.
	//
	// Corresponds with GET /api/version (the `GetVersion` operationId).
	GetVersion(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EnsureWebhookWithBody Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
	//
	// Creates a webhook on the named repo, pointed at the signed-in
	// user's own webhook URL and secret, subscribed to the events
	// docs/webhooks.md's manual steps recommend. If one already exists
	// there (found the same way GET /api/dashboard's own `hasWebhook`
	// does — matching by URL path, not object identity) it's brought
	// back to active with the right events instead of creating a
	// duplicate.
	//
	// One of a small number of endpoints in this API that write to a
	// forge rather than just reading from it — see also the
	// pull-requests tag.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
	EnsureWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EnsureWebhook Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
	//
	// Creates a webhook on the named repo, pointed at the signed-in
	// user's own webhook URL and secret, subscribed to the events
	// docs/webhooks.md's manual steps recommend. If one already exists
	// there (found the same way GET /api/dashboard's own `hasWebhook`
	// does — matching by URL path, not object identity) it's brought
	// back to active with the right events instead of creating a
	// duplicate.
	//
	// One of a small number of endpoints in this API that write to a
	// forge rather than just reading from it — see also the
	// pull-requests tag.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
	EnsureWebhook(ctx context.Context, body EnsureWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReceiveForgejoWebhookWithBody Receive a Forgejo repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's signature header is verified against that
	// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
	// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
	// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
	// delivery means the same thing here — something changed, refresh —
	// so nothing about the payload itself is parsed.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
	ReceiveForgejoWebhookWithBody(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReceiveForgejoWebhook Receive a Forgejo repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's signature header is verified against that
	// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
	// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
	// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
	// delivery means the same thing here — something changed, refresh —
	// so nothing about the payload itself is parsed.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
	ReceiveForgejoWebhook(ctx context.Context, webhookToken PathWebhookToken, body ReceiveForgejoWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReceiveGitHubWebhookWithBody Receive a GitHub repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's `X-Hub-Signature-256` header is verified against
	// that user's webhook secret. Every delivery GitHub can send for a
	// repository webhook (a new pull request, a closed issue, a status or
	// check-run update, even the `ping` event GitHub sends when the
	// webhook is first created) means the same thing here — something
	// changed, refresh — so nothing about the payload itself is parsed.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
	ReceiveGitHubWebhookWithBody(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReceiveGitHubWebhook Receive a GitHub repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's `X-Hub-Signature-256` header is verified against
	// that user's webhook secret. Every delivery GitHub can send for a
	// repository webhook (a new pull request, a closed issue, a status or
	// check-run update, even the `ping` event GitHub sends when the
	// webhook is first created) means the same thing here — something
	// changed, refresh — so nothing about the payload itself is parsed.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
	ReceiveGitHubWebhook(ctx context.Context, webhookToken PathWebhookToken, body ReceiveGitHubWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Health Liveness
	//
	// Answers 200 once the server has started. Doesn't reflect whether
	// either forge is reachable — see forges[].reachable on the dashboard
	// response for that.
	//
	// Corresponds with GET /healthz (the `Health` operationId).
	Health(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// ListUsers List every registered user
//
// Never includes a passkey or a forge credential — a username,
// display name, admin flag and registration timestamp per user.
//
// Corresponds with GET /api/admin/users (the `ListUsers` operationId).
func (c *Client) ListUsers(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListUsersRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteUser Remove a user outright
//
// Deletes the account, every session, every registered passkey, and
// every saved forge credential — irreversible. Their username
// becomes available for a fresh registration.
//
// Corresponds with DELETE /api/admin/users/{username} (the `DeleteUser` operationId).
func (c *Client) DeleteUser(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteUserRequest(c.Server, username)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RevokeUser Revoke a user's passkeys and sessions
//
// Deletes every session and every registered passkey for this user —
// they're signed out everywhere immediately, and have to register a
// new passkey from scratch to get back in. Their saved forge
// credentials are untouched; use the delete endpoint to remove the
// account itself.
//
// Corresponds with POST /api/admin/users/{username}/revoke (the `RevokeUser` operationId).
func (c *Client) RevokeUser(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRevokeUserRequest(c.Server, username)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListCredentials List the signed-in user's own passkeys
//
// Every passkey on the account (#355), oldest first — not just the
// one used to establish the current session.
//
// Corresponds with GET /api/auth/credentials (the `ListCredentials` operationId).
func (c *Client) ListCredentials(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListCredentialsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BeginAddCredential Start a ceremony to add another passkey to the signed-in account
//
// The authenticated counterpart to POST /api/auth/register/begin —
// that one only ever works for a brand-new, credential-less
// account; this is how an already-registered user adds a second
// (or third...) passkey, e.g. a laptop and a security key (#355).
//
// Corresponds with POST /api/auth/credentials/begin (the `BeginAddCredential` operationId).
func (c *Client) BeginAddCredential(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBeginAddCredentialRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishAddCredentialWithBody Complete an add-credential ceremony
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
func (c *Client) FinishAddCredentialWithBody(ctx context.Context, params *FinishAddCredentialParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishAddCredentialRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishAddCredential Complete an add-credential ceremony
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
func (c *Client) FinishAddCredential(ctx context.Context, params *FinishAddCredentialParams, body FinishAddCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishAddCredentialRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteCredential Remove one of the signed-in user's own passkeys
//
// Refused on the account's last remaining passkey (#355) — this
// app is WebAuthn-only with no password fallback, so deleting it
// would lock the account out entirely.
//
// Corresponds with DELETE /api/auth/credentials/{id} (the `DeleteCredential` operationId).
func (c *Client) DeleteCredential(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteCredentialRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BeginLoginWithBody Start a passkey login ceremony
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
func (c *Client) BeginLoginWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBeginLoginRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BeginLogin Start a passkey login ceremony
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
func (c *Client) BeginLogin(ctx context.Context, body BeginLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBeginLoginRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishLoginWithBody Complete a passkey login ceremony
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
func (c *Client) FinishLoginWithBody(ctx context.Context, params *FinishLoginParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishLoginRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishLogin Complete a passkey login ceremony
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
func (c *Client) FinishLogin(ctx context.Context, params *FinishLoginParams, body FinishLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishLoginRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Logout End the current session
//
// Corresponds with POST /api/auth/logout (the `Logout` operationId).
func (c *Client) Logout(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewLogoutRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BeginRegistrationWithBody Start a passkey registration ceremony
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
func (c *Client) BeginRegistrationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBeginRegistrationRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BeginRegistration Start a passkey registration ceremony
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
func (c *Client) BeginRegistration(ctx context.Context, body BeginRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBeginRegistrationRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishRegistrationWithBody Complete a passkey registration ceremony
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
func (c *Client) FinishRegistrationWithBody(ctx context.Context, params *FinishRegistrationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishRegistrationRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// FinishRegistration Complete a passkey registration ceremony
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
func (c *Client) FinishRegistration(ctx context.Context, params *FinishRegistrationParams, body FinishRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewFinishRegistrationRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSession Who, if anyone, the current session cookie belongs to
//
// Corresponds with GET /api/auth/session (the `GetSession` operationId).
func (c *Client) GetSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSessionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetDashboard The aggregated view
//
// Returns the most recently fetched snapshot. The backend refreshes
// this on its own schedule in the background; this endpoint never
// blocks on a live call to either forge, so it answers instantly even
// when a forge is slow or unreachable.
//
// Omitting `owner` returns the signed-in user's own dashboard. Passing
// it returns that user's dashboard instead — allowed only when they've
// shared it with the caller (or the caller is viewing their own
// username), and refused with a 403 otherwise.
//
// Corresponds with GET /api/dashboard (the `GetDashboard` operationId).
func (c *Client) GetDashboard(ctx context.Context, params *GetDashboardParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetDashboardRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RefreshDashboard Trigger an immediate refresh of the signed-in user's own dashboard
//
// Blocks until an immediate, out-of-band refresh of the signed-in
// user's own dashboard completes, then returns the resulting
// snapshot — for retrying right away after a forge was only
// briefly unreachable, instead of waiting for the next scheduled
// background refresh.
//
// Doesn't support `owner` the way GET /api/dashboard does — only
// ever the signed-in user's own dashboard, never a shared one, so
// a user with shared access to someone else's dashboard can never
// spend that owner's own forge rate-limit budget on their own
// schedule.
//
// Corresponds with POST /api/dashboard/refresh (the `RefreshDashboard` operationId).
func (c *Client) RefreshDashboard(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRefreshDashboardRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// StreamDashboard Server-Sent Events stream of the signed-in user's own dashboard
//
// Pushes a fresh snapshot every time this user's background
// refresh produces one — most notably right after a verified
// webhook delivery (see the webhooks tag) triggers an immediate
// one, which is what turns that into a live update instead of
// something only the next scheduled refresh picks up.
//
// Doesn't support `owner` the way GET /api/dashboard does — only
// ever the signed-in user's own dashboard, never a shared one.
// The frontend's own 30-second poll against GET /api/dashboard
// keeps running unconditionally, connected or not — a browser or
// proxy that can't hold an SSE connection open just never
// benefits from this, rather than the dashboard going stale
// silently.
//
// Corresponds with GET /api/dashboard/stream (the `StreamDashboard` operationId).
func (c *Client) StreamDashboard(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewStreamDashboardRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetPullRequestChecks List every job/check run against a pull request's head commit, on demand
//
// Fetched live on every call, never as part of GET /api/dashboard's
// own snapshot — this is detail a pull request's row doesn't show
// until a caller actually asks for it, and adding it to the eager
// refresh loop would multiply that refresh's cost by every open
// pull request for data most of them are never opened for.
//
// Falls back to the legacy combined commit-status API's own
// per-check entries when the forge reports no Actions runs at all
// for that commit (Actions disabled, or an instance too old to
// expose them) — never an error for that case specifically, since
// it's a normal, expected shape for an older or CI-less repo.
//
// Corresponds with GET /api/pull-requests/checks (the `GetPullRequestChecks` operationId).
func (c *Client) GetPullRequestChecks(ctx context.Context, params *GetPullRequestChecksParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetPullRequestChecksRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PostPullRequestDependabotActionWithBody Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
//
// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
// comment on the named pull request — Dependabot's own documented
// comment-command interface, not a generic "post any comment"
// endpoint; `action` is validated server-side to one of the two
// values below and nothing else is ever sent. GitHub only —
// Dependabot doesn't run on Forgejo.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
func (c *Client) PostPullRequestDependabotActionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPostPullRequestDependabotActionRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PostPullRequestDependabotAction Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
//
// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
// comment on the named pull request — Dependabot's own documented
// comment-command interface, not a generic "post any comment"
// endpoint; `action` is validated server-side to one of the two
// values below and nothing else is ever sent. GitHub only —
// Dependabot doesn't run on Forgejo.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
func (c *Client) PostPullRequestDependabotAction(ctx context.Context, body PostPullRequestDependabotActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPostPullRequestDependabotActionRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MergePullRequestWithBody Merge one pull request, on the signed-in user's behalf
//
// Merges the named pull request using its repo's own configured
// default merge method — GitHub is asked to pick its own repo
// default; Forgejo's API has no such default built in, so this
// looks up the repo's configured default merge style first and
// passes that explicitly. Neither takes a method override here;
// picking one is out of scope for this endpoint.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
func (c *Client) MergePullRequestWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMergePullRequestRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MergePullRequest Merge one pull request, on the signed-in user's behalf
//
// Merges the named pull request using its repo's own configured
// default merge method — GitHub is asked to pick its own repo
// default; Forgejo's API has no such default built in, so this
// looks up the repo's configured default merge style first and
// passes that explicitly. Neither takes a method override here;
// picking one is out of scope for this endpoint.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
func (c *Client) MergePullRequest(ctx context.Context, body MergePullRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMergePullRequestRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PostPullRequestRenovateRebaseWithBody Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
//
// Adds the signed-in user's own configured Renovate rebase label
// (Settings' "Renovate rebase label" field, defaulting to
// Renovate's own `rebase` if left blank) to the named pull
// request — Renovate's own documented rebase/retry trigger, which
// is a label rather than a comment command. Works on both GitHub
// and Forgejo, unlike the Dependabot actions, since Renovate runs
// on both.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
func (c *Client) PostPullRequestRenovateRebaseWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPostPullRequestRenovateRebaseRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PostPullRequestRenovateRebase Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
//
// Adds the signed-in user's own configured Renovate rebase label
// (Settings' "Renovate rebase label" field, defaulting to
// Renovate's own `rebase` if left blank) to the named pull
// request — Renovate's own documented rebase/retry trigger, which
// is a label rather than a comment command. Works on both GitHub
// and Forgejo, unlike the Dependabot actions, since Renovate runs
// on both.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
func (c *Client) PostPullRequestRenovateRebase(ctx context.Context, body PostPullRequestRenovateRebaseJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPostPullRequestRenovateRebaseRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePullRequestBranchWithBody Bring one pull request's branch up to date with its base, on the signed-in user's behalf
//
// Merges the named pull request's base branch into its own head
// branch. GitHub can schedule this as a background job rather than
// finishing it inline — a 202 here means exactly that, not a
// failure; a follow-up refresh a moment later reflects the result.
// Forgejo's equivalent is always synchronous, so it only ever
// answers 204.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
func (c *Client) UpdatePullRequestBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePullRequestBranchRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePullRequestBranch Bring one pull request's branch up to date with its base, on the signed-in user's behalf
//
// Merges the named pull request's base branch into its own head
// branch. GitHub can schedule this as a background job rather than
// finishing it inline — a 202 here means exactly that, not a
// failure; a follow-up refresh a moment later reflects the result.
// Forgejo's equivalent is always synchronous, so it only ever
// answers 204.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
func (c *Client) UpdatePullRequestBranch(ctx context.Context, body UpdatePullRequestBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePullRequestBranchRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DisableAutoUpdateBranchWithBody Turn off automatic branch updates for one tracked repo
//
// The reverse of POST /api/repos/auto-update-branch/enable.
// Idempotent: disabling a repo that was never enabled is a no-op,
// not an error.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
func (c *Client) DisableAutoUpdateBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDisableAutoUpdateBranchRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DisableAutoUpdateBranch Turn off automatic branch updates for one tracked repo
//
// The reverse of POST /api/repos/auto-update-branch/enable.
// Idempotent: disabling a repo that was never enabled is a no-op,
// not an error.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
func (c *Client) DisableAutoUpdateBranch(ctx context.Context, body DisableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDisableAutoUpdateBranchRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EnableAutoUpdateBranchWithBody Turn on automatic branch updates for one tracked repo
//
// Any of this repo's pull requests the background refresh finds
// behind its base branch gets updated automatically from then on
// (#365), the same as clicking "Update branch" would — suppressed
// for a bot-managed pull request unless bot-PR updates are
// separately allowed. Idempotent: enabling an already-enabled repo
// is a no-op, not an error.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
func (c *Client) EnableAutoUpdateBranchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEnableAutoUpdateBranchRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EnableAutoUpdateBranch Turn on automatic branch updates for one tracked repo
//
// Any of this repo's pull requests the background refresh finds
// behind its base branch gets updated automatically from then on
// (#365), the same as clicking "Update branch" would — suppressed
// for a bot-managed pull request unless bot-PR updates are
// separately allowed. Idempotent: enabling an already-enabled repo
// is a no-op, not an error.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
func (c *Client) EnableAutoUpdateBranch(ctx context.Context, body EnableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEnableAutoUpdateBranchRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// IgnoreRepoWithBody Hide one tracked repo's pull requests and issues from the dashboard and Insights
//
// Reversible, not destructive (#363): the repo itself keeps
// appearing in GET /api/dashboard's `repos` array with accurate
// webhook-coverage status, and keeps being fetched and counted —
// only its pullRequests/issues entries stop appearing there and on
// Insights. Idempotent: ignoring an already-ignored repo is a
// no-op, not an error.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
func (c *Client) IgnoreRepoWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewIgnoreRepoRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// IgnoreRepo Hide one tracked repo's pull requests and issues from the dashboard and Insights
//
// Reversible, not destructive (#363): the repo itself keeps
// appearing in GET /api/dashboard's `repos` array with accurate
// webhook-coverage status, and keeps being fetched and counted —
// only its pullRequests/issues entries stop appearing there and on
// Insights. Idempotent: ignoring an already-ignored repo is a
// no-op, not an error.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
func (c *Client) IgnoreRepo(ctx context.Context, body IgnoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewIgnoreRepoRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UnignoreRepoWithBody Stop ignoring one tracked repo
//
// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
// repo that was never ignored is a no-op, not an error.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
func (c *Client) UnignoreRepoWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUnignoreRepoRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UnignoreRepo Stop ignoring one tracked repo
//
// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
// repo that was never ignored is a no-op, not an error.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
func (c *Client) UnignoreRepo(ctx context.Context, body UnignoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUnignoreRepoRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSettings The signed-in user's own forge configuration
//
// Tokens are never returned, before or after being saved — only
// whether one is currently on file. There is no way to read a saved
// token back out through this API.
//
// Corresponds with GET /api/settings (the `GetSettings` operationId).
func (c *Client) GetSettings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSettingsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PutSettingsWithBody Save the signed-in user's forge configuration
//
// Replaces every field except a blank token, which means "keep
// whatever's already saved" rather than "clear it" — the only
// sensible behavior for a field the browser is never shown the
// current value of. Saving triggers that user's dashboard to start
// refreshing from the new configuration.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /api/settings (the `PutSettings` operationId).
func (c *Client) PutSettingsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPutSettingsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PutSettings Save the signed-in user's forge configuration
//
// Replaces every field except a blank token, which means "keep
// whatever's already saved" rather than "clear it" — the only
// sensible behavior for a field the browser is never shown the
// current value of. Saving triggers that user's dashboard to start
// refreshing from the new configuration.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /api/settings (the `PutSettings` operationId).
func (c *Client) PutSettings(ctx context.Context, body PutSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPutSettingsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBotPrUpdatesSetting Whether bot-managed pull request branches can be updated
//
// A lightweight, side-effect-free read of one field —
// allowBotPrUpdates — for the dashboard page to check on every
// load. Deliberately not GET /api/settings itself: that handler
// also provisions webhook credentials on first call
// (EnsureWebhookCredentials), which the dashboard visiting on a
// user's behalf shouldn't trigger for someone who's never opened
// Settings at all.
//
// Corresponds with GET /api/settings/bot-pr-updates (the `GetBotPrUpdatesSetting` operationId).
func (c *Client) GetBotPrUpdatesSetting(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBotPrUpdatesSettingRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetFilterState The signed-in user's own saved dashboard/Insights filter state
//
// A lightweight read of one opaque blob (#353), the same
// "deliberately not GET /api/settings itself" restraint
// GET /api/settings/bot-pr-updates already uses, for the same
// reason: this loads on every dashboard/Insights visit and
// shouldn't provision webhook credentials as a side effect.
// `{}` for a user who's never saved any filters yet, not a 404.
//
// Corresponds with GET /api/settings/filter-state (the `GetFilterState` operationId).
func (c *Client) GetFilterState(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetFilterStateRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetFilterStateWithBody Save the signed-in user's own dashboard/Insights filter state
//
// A dedicated, lightweight save separate from the main
// PUT /api/settings — filters change on nearly every click, a
// mismatch for that endpoint's "always a full form submit"
// convention (#353). The body replaces the saved state entirely,
// the same "always a full submit, just of a much smaller and
// much more frequent thing" shape as the main settings PUT, not a
// partial patch. Filters.js's own client-side code decides when
// to call this: immediately for a discrete control, debounced
// while the user is still typing in the free-text Title filter.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
func (c *Client) SetFilterStateWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetFilterStateRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetFilterState Save the signed-in user's own dashboard/Insights filter state
//
// A dedicated, lightweight save separate from the main
// PUT /api/settings — filters change on nearly every click, a
// mismatch for that endpoint's "always a full form submit"
// convention (#353). The body replaces the saved state entirely,
// the same "always a full submit, just of a much smaller and
// much more frequent thing" shape as the main settings PUT, not a
// partial patch. Filters.js's own client-side code decides when
// to call this: immediately for a discrete control, debounced
// while the user is still typing in the free-text Title filter.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
func (c *Client) SetFilterState(ctx context.Context, body SetFilterStateJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetFilterStateRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetTheme The signed-in user's own saved theme preference
//
// A lightweight, side-effect-free read of one field — theme — for
// every page to check on load (#352). The same "deliberately not
// GET /api/settings itself" restraint GET /api/settings/bot-pr-updates
// already uses, for the same reason: this loads on every page visit
// and shouldn't provision webhook credentials as a side effect.
//
// Corresponds with GET /api/settings/theme (the `GetTheme` operationId).
func (c *Client) GetTheme(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetThemeRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetThemeWithBody Save the signed-in user's own theme preference
//
// A dedicated, instant save (#352) — deliberately not routed
// through the main PUT /api/settings, whose every other field is
// a plain replace rather than a per-field merge: a request
// carrying only theme through that handler would blank every
// other saved setting. Picking a theme applies immediately, the
// same "set once and forget" convention the ticket's own research
// cites, rather than waiting on the rest of the settings form's
// own Save button.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
func (c *Client) SetThemeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetThemeRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetTheme Save the signed-in user's own theme preference
//
// A dedicated, instant save (#352) — deliberately not routed
// through the main PUT /api/settings, whose every other field is
// a plain replace rather than a per-field merge: a request
// carrying only theme through that handler would blank every
// other saved setting. Picking a theme applies immediately, the
// same "set once and forget" convention the ticket's own research
// cites, rather than waiting on the rest of the settings form's
// own Save button.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
func (c *Client) SetTheme(ctx context.Context, body SetThemeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetThemeRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSharing The signed-in user's sharing relationships
//
// Both directions at once: who the signed-in user has shared their
// own dashboard with, and whose dashboards have been shared with
// them.
//
// Corresponds with GET /api/sharing (the `GetSharing` operationId).
func (c *Client) GetSharing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSharingRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UnshareWith Stop sharing the signed-in user's dashboard with username
//
// Idempotent — removing a username that was never shared with changes nothing.
//
// Corresponds with DELETE /api/sharing/{username} (the `UnshareWith` operationId).
func (c *Client) UnshareWith(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUnshareWithRequest(c.Server, username)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ShareWith Share the signed-in user's dashboard with username
//
// Idempotent — sharing with a username already on the list changes
// nothing. username needs no tokens of their own configured to view
// the shared dashboard; they see the owner's aggregated data exactly
// as the owner sees it.
//
// Corresponds with PUT /api/sharing/{username} (the `ShareWith` operationId).
func (c *Client) ShareWith(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewShareWithRequest(c.Server, username)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAPITokens List the signed-in user's own personal API tokens
//
// Metadata only, per APIToken's own description — a raw value never reappears once created.
//
// Corresponds with GET /api/tokens (the `ListAPITokens` operationId).
func (c *Client) ListAPITokens(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAPITokensRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAPITokenWithBody Generate a new personal API token
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
func (c *Client) CreateAPITokenWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAPITokenRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAPIToken Generate a new personal API token
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
func (c *Client) CreateAPIToken(ctx context.Context, body CreateAPITokenJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAPITokenRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteAPIToken Revoke a personal API token
//
// Stops the token authenticating immediately. Idempotent — deleting
// an id that's already gone (or never belonged to the signed-in
// user) still answers 204, the same as any other delete-by-id
// endpoint here (compare DELETE /api/sharing/{username}), so a
// double-click or a retried request isn't an error.
//
// Corresponds with DELETE /api/tokens/{id} (the `DeleteAPIToken` operationId).
func (c *Client) DeleteAPIToken(ctx context.Context, id PathTokenID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteAPITokenRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetVersion The running server's own version
//
// The tag goreleaser stamped the running binary with ("dev" for a
// build outside the release pipeline). Public and unauthenticated —
// every page, including the pre-login screen, links to the matching
// GitHub release from its footer so a person can tell what's
// actually deployed without shelling into the box.
//
// Corresponds with GET /api/version (the `GetVersion` operationId).
func (c *Client) GetVersion(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetVersionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EnsureWebhookWithBody Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
//
// Creates a webhook on the named repo, pointed at the signed-in
// user's own webhook URL and secret, subscribed to the events
// docs/webhooks.md's manual steps recommend. If one already exists
// there (found the same way GET /api/dashboard's own `hasWebhook`
// does — matching by URL path, not object identity) it's brought
// back to active with the right events instead of creating a
// duplicate.
//
// One of a small number of endpoints in this API that write to a
// forge rather than just reading from it — see also the
// pull-requests tag.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
func (c *Client) EnsureWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEnsureWebhookRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EnsureWebhook Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
//
// Creates a webhook on the named repo, pointed at the signed-in
// user's own webhook URL and secret, subscribed to the events
// docs/webhooks.md's manual steps recommend. If one already exists
// there (found the same way GET /api/dashboard's own `hasWebhook`
// does — matching by URL path, not object identity) it's brought
// back to active with the right events instead of creating a
// duplicate.
//
// One of a small number of endpoints in this API that write to a
// forge rather than just reading from it — see also the
// pull-requests tag.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
func (c *Client) EnsureWebhook(ctx context.Context, body EnsureWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEnsureWebhookRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReceiveForgejoWebhookWithBody Receive a Forgejo repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's signature header is verified against that
// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
// delivery means the same thing here — something changed, refresh —
// so nothing about the payload itself is parsed.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
func (c *Client) ReceiveForgejoWebhookWithBody(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveForgejoWebhookRequestWithBody(c.Server, webhookToken, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReceiveForgejoWebhook Receive a Forgejo repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's signature header is verified against that
// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
// delivery means the same thing here — something changed, refresh —
// so nothing about the payload itself is parsed.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
func (c *Client) ReceiveForgejoWebhook(ctx context.Context, webhookToken PathWebhookToken, body ReceiveForgejoWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveForgejoWebhookRequest(c.Server, webhookToken, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReceiveGitHubWebhookWithBody Receive a GitHub repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's `X-Hub-Signature-256` header is verified against
// that user's webhook secret. Every delivery GitHub can send for a
// repository webhook (a new pull request, a closed issue, a status or
// check-run update, even the `ping` event GitHub sends when the
// webhook is first created) means the same thing here — something
// changed, refresh — so nothing about the payload itself is parsed.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
func (c *Client) ReceiveGitHubWebhookWithBody(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveGitHubWebhookRequestWithBody(c.Server, webhookToken, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReceiveGitHubWebhook Receive a GitHub repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's `X-Hub-Signature-256` header is verified against
// that user's webhook secret. Every delivery GitHub can send for a
// repository webhook (a new pull request, a closed issue, a status or
// check-run update, even the `ping` event GitHub sends when the
// webhook is first created) means the same thing here — something
// changed, refresh — so nothing about the payload itself is parsed.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
func (c *Client) ReceiveGitHubWebhook(ctx context.Context, webhookToken PathWebhookToken, body ReceiveGitHubWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveGitHubWebhookRequest(c.Server, webhookToken, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Health Liveness
//
// Answers 200 once the server has started. Doesn't reflect whether
// either forge is reachable — see forges[].reachable on the dashboard
// response for that.
//
// Corresponds with GET /healthz (the `Health` operationId).
func (c *Client) Health(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewListUsersRequest constructs an http.Request for the ListUsers method
func NewListUsersRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/admin/users")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteUserRequest constructs an http.Request for the DeleteUser method
func NewDeleteUserRequest(server string, username PathUsername) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "username", username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/admin/users/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRevokeUserRequest constructs an http.Request for the RevokeUser method
func NewRevokeUserRequest(server string, username PathUsername) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "username", username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/admin/users/%s/revoke", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListCredentialsRequest constructs an http.Request for the ListCredentials method
func NewListCredentialsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBeginAddCredentialRequest constructs an http.Request for the BeginAddCredential method
func NewBeginAddCredentialRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/credentials/begin")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewFinishAddCredentialRequest calls the generic FinishAddCredential builder with application/json body
func NewFinishAddCredentialRequest(server string, params *FinishAddCredentialParams, body FinishAddCredentialJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewFinishAddCredentialRequestWithBody(server, params, "application/json", bodyReader)
}

// NewFinishAddCredentialRequestWithBody constructs an http.Request for the FinishAddCredential method, with any body, and a specified content type
func NewFinishAddCredentialRequestWithBody(server string, params *FinishAddCredentialParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/credentials/finish")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "label", params.Label, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteCredentialRequest constructs an http.Request for the DeleteCredential method
func NewDeleteCredentialRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/credentials/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBeginLoginRequest calls the generic BeginLogin builder with application/json body
func NewBeginLoginRequest(server string, body BeginLoginJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBeginLoginRequestWithBody(server, "application/json", bodyReader)
}

// NewBeginLoginRequestWithBody constructs an http.Request for the BeginLogin method, with any body, and a specified content type
func NewBeginLoginRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/login/begin")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewFinishLoginRequest calls the generic FinishLogin builder with application/json body
func NewFinishLoginRequest(server string, params *FinishLoginParams, body FinishLoginJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewFinishLoginRequestWithBody(server, params, "application/json", bodyReader)
}

// NewFinishLoginRequestWithBody constructs an http.Request for the FinishLogin method, with any body, and a specified content type
func NewFinishLoginRequestWithBody(server string, params *FinishLoginParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/login/finish")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "username", params.Username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewLogoutRequest constructs an http.Request for the Logout method
func NewLogoutRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/logout")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBeginRegistrationRequest calls the generic BeginRegistration builder with application/json body
func NewBeginRegistrationRequest(server string, body BeginRegistrationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBeginRegistrationRequestWithBody(server, "application/json", bodyReader)
}

// NewBeginRegistrationRequestWithBody constructs an http.Request for the BeginRegistration method, with any body, and a specified content type
func NewBeginRegistrationRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/register/begin")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewFinishRegistrationRequest calls the generic FinishRegistration builder with application/json body
func NewFinishRegistrationRequest(server string, params *FinishRegistrationParams, body FinishRegistrationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewFinishRegistrationRequestWithBody(server, params, "application/json", bodyReader)
}

// NewFinishRegistrationRequestWithBody constructs an http.Request for the FinishRegistration method, with any body, and a specified content type
func NewFinishRegistrationRequestWithBody(server string, params *FinishRegistrationParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/register/finish")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "username", params.Username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetSessionRequest constructs an http.Request for the GetSession method
func NewGetSessionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/auth/session")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetDashboardRequest constructs an http.Request for the GetDashboard method
func NewGetDashboardRequest(server string, params *GetDashboardParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/dashboard")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Owner != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "owner", *params.Owner, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRefreshDashboardRequest constructs an http.Request for the RefreshDashboard method
func NewRefreshDashboardRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/dashboard/refresh")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewStreamDashboardRequest constructs an http.Request for the StreamDashboard method
func NewStreamDashboardRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/dashboard/stream")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetPullRequestChecksRequest constructs an http.Request for the GetPullRequestChecks method
func NewGetPullRequestChecksRequest(server string, params *GetPullRequestChecksParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/pull-requests/checks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "forge", params.Forge, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "fullName", params.FullName, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "number", params.Number, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewPostPullRequestDependabotActionRequest calls the generic PostPullRequestDependabotAction builder with application/json body
func NewPostPullRequestDependabotActionRequest(server string, body PostPullRequestDependabotActionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPostPullRequestDependabotActionRequestWithBody(server, "application/json", bodyReader)
}

// NewPostPullRequestDependabotActionRequestWithBody constructs an http.Request for the PostPullRequestDependabotAction method, with any body, and a specified content type
func NewPostPullRequestDependabotActionRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/pull-requests/dependabot-action")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewMergePullRequestRequest calls the generic MergePullRequest builder with application/json body
func NewMergePullRequestRequest(server string, body MergePullRequestJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewMergePullRequestRequestWithBody(server, "application/json", bodyReader)
}

// NewMergePullRequestRequestWithBody constructs an http.Request for the MergePullRequest method, with any body, and a specified content type
func NewMergePullRequestRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/pull-requests/merge")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPostPullRequestRenovateRebaseRequest calls the generic PostPullRequestRenovateRebase builder with application/json body
func NewPostPullRequestRenovateRebaseRequest(server string, body PostPullRequestRenovateRebaseJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPostPullRequestRenovateRebaseRequestWithBody(server, "application/json", bodyReader)
}

// NewPostPullRequestRenovateRebaseRequestWithBody constructs an http.Request for the PostPullRequestRenovateRebase method, with any body, and a specified content type
func NewPostPullRequestRenovateRebaseRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/pull-requests/renovate-rebase")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewUpdatePullRequestBranchRequest calls the generic UpdatePullRequestBranch builder with application/json body
func NewUpdatePullRequestBranchRequest(server string, body UpdatePullRequestBranchJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdatePullRequestBranchRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdatePullRequestBranchRequestWithBody constructs an http.Request for the UpdatePullRequestBranch method, with any body, and a specified content type
func NewUpdatePullRequestBranchRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/pull-requests/update-branch")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDisableAutoUpdateBranchRequest calls the generic DisableAutoUpdateBranch builder with application/json body
func NewDisableAutoUpdateBranchRequest(server string, body DisableAutoUpdateBranchJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewDisableAutoUpdateBranchRequestWithBody(server, "application/json", bodyReader)
}

// NewDisableAutoUpdateBranchRequestWithBody constructs an http.Request for the DisableAutoUpdateBranch method, with any body, and a specified content type
func NewDisableAutoUpdateBranchRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/repos/auto-update-branch/disable")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewEnableAutoUpdateBranchRequest calls the generic EnableAutoUpdateBranch builder with application/json body
func NewEnableAutoUpdateBranchRequest(server string, body EnableAutoUpdateBranchJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewEnableAutoUpdateBranchRequestWithBody(server, "application/json", bodyReader)
}

// NewEnableAutoUpdateBranchRequestWithBody constructs an http.Request for the EnableAutoUpdateBranch method, with any body, and a specified content type
func NewEnableAutoUpdateBranchRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/repos/auto-update-branch/enable")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewIgnoreRepoRequest calls the generic IgnoreRepo builder with application/json body
func NewIgnoreRepoRequest(server string, body IgnoreRepoJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewIgnoreRepoRequestWithBody(server, "application/json", bodyReader)
}

// NewIgnoreRepoRequestWithBody constructs an http.Request for the IgnoreRepo method, with any body, and a specified content type
func NewIgnoreRepoRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/repos/ignore")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewUnignoreRepoRequest calls the generic UnignoreRepo builder with application/json body
func NewUnignoreRepoRequest(server string, body UnignoreRepoJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUnignoreRepoRequestWithBody(server, "application/json", bodyReader)
}

// NewUnignoreRepoRequestWithBody constructs an http.Request for the UnignoreRepo method, with any body, and a specified content type
func NewUnignoreRepoRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/repos/unignore")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetSettingsRequest constructs an http.Request for the GetSettings method
func NewGetSettingsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewPutSettingsRequest calls the generic PutSettings builder with application/json body
func NewPutSettingsRequest(server string, body PutSettingsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPutSettingsRequestWithBody(server, "application/json", bodyReader)
}

// NewPutSettingsRequestWithBody constructs an http.Request for the PutSettings method, with any body, and a specified content type
func NewPutSettingsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetBotPrUpdatesSettingRequest constructs an http.Request for the GetBotPrUpdatesSetting method
func NewGetBotPrUpdatesSettingRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings/bot-pr-updates")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetFilterStateRequest constructs an http.Request for the GetFilterState method
func NewGetFilterStateRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings/filter-state")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSetFilterStateRequest calls the generic SetFilterState builder with application/json body
func NewSetFilterStateRequest(server string, body SetFilterStateJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetFilterStateRequestWithBody(server, "application/json", bodyReader)
}

// NewSetFilterStateRequestWithBody constructs an http.Request for the SetFilterState method, with any body, and a specified content type
func NewSetFilterStateRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings/filter-state")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetThemeRequest constructs an http.Request for the GetTheme method
func NewGetThemeRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings/theme")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSetThemeRequest calls the generic SetTheme builder with application/json body
func NewSetThemeRequest(server string, body SetThemeJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetThemeRequestWithBody(server, "application/json", bodyReader)
}

// NewSetThemeRequestWithBody constructs an http.Request for the SetTheme method, with any body, and a specified content type
func NewSetThemeRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/settings/theme")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetSharingRequest constructs an http.Request for the GetSharing method
func NewGetSharingRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/sharing")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUnshareWithRequest constructs an http.Request for the UnshareWith method
func NewUnshareWithRequest(server string, username PathUsername) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "username", username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/sharing/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewShareWithRequest constructs an http.Request for the ShareWith method
func NewShareWithRequest(server string, username PathUsername) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "username", username, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/sharing/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAPITokensRequest constructs an http.Request for the ListAPITokens method
func NewListAPITokensRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/tokens")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateAPITokenRequest calls the generic CreateAPIToken builder with application/json body
func NewCreateAPITokenRequest(server string, body CreateAPITokenJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateAPITokenRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateAPITokenRequestWithBody constructs an http.Request for the CreateAPIToken method, with any body, and a specified content type
func NewCreateAPITokenRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/tokens")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteAPITokenRequest constructs an http.Request for the DeleteAPIToken method
func NewDeleteAPITokenRequest(server string, id PathTokenID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/tokens/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetVersionRequest constructs an http.Request for the GetVersion method
func NewGetVersionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/version")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewEnsureWebhookRequest calls the generic EnsureWebhook builder with application/json body
func NewEnsureWebhookRequest(server string, body EnsureWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewEnsureWebhookRequestWithBody(server, "application/json", bodyReader)
}

// NewEnsureWebhookRequestWithBody constructs an http.Request for the EnsureWebhook method, with any body, and a specified content type
func NewEnsureWebhookRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/webhooks/ensure")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewReceiveForgejoWebhookRequest calls the generic ReceiveForgejoWebhook builder with application/json body
func NewReceiveForgejoWebhookRequest(server string, webhookToken PathWebhookToken, body ReceiveForgejoWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReceiveForgejoWebhookRequestWithBody(server, webhookToken, "application/json", bodyReader)
}

// NewReceiveForgejoWebhookRequestWithBody constructs an http.Request for the ReceiveForgejoWebhook method, with any body, and a specified content type
func NewReceiveForgejoWebhookRequestWithBody(server string, webhookToken PathWebhookToken, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "webhookToken", webhookToken, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/webhooks/forgejo/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewReceiveGitHubWebhookRequest calls the generic ReceiveGitHubWebhook builder with application/json body
func NewReceiveGitHubWebhookRequest(server string, webhookToken PathWebhookToken, body ReceiveGitHubWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReceiveGitHubWebhookRequestWithBody(server, webhookToken, "application/json", bodyReader)
}

// NewReceiveGitHubWebhookRequestWithBody constructs an http.Request for the ReceiveGitHubWebhook method, with any body, and a specified content type
func NewReceiveGitHubWebhookRequestWithBody(server string, webhookToken PathWebhookToken, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "webhookToken", webhookToken, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/webhooks/github/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewHealthRequest constructs an http.Request for the Health method
func NewHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/healthz")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// ListUsersWithResponse List every registered user
	//
	// Never includes a passkey or a forge credential — a username,
	// display name, admin flag and registration timestamp per user.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/admin/users (the `ListUsers` operationId).
	ListUsersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListUsersResponse, error)

	// DeleteUserWithResponse Remove a user outright
	//
	// Deletes the account, every session, every registered passkey, and
	// every saved forge credential — irreversible. Their username
	// becomes available for a fresh registration.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/admin/users/{username} (the `DeleteUser` operationId).
	DeleteUserWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*DeleteUserResponse, error)

	// RevokeUserWithResponse Revoke a user's passkeys and sessions
	//
	// Deletes every session and every registered passkey for this user —
	// they're signed out everywhere immediately, and have to register a
	// new passkey from scratch to get back in. Their saved forge
	// credentials are untouched; use the delete endpoint to remove the
	// account itself.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/admin/users/{username}/revoke (the `RevokeUser` operationId).
	RevokeUserWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*RevokeUserResponse, error)

	// ListCredentialsWithResponse List the signed-in user's own passkeys
	//
	// Every passkey on the account (#355), oldest first — not just the
	// one used to establish the current session.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/auth/credentials (the `ListCredentials` operationId).
	ListCredentialsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListCredentialsResponse, error)

	// BeginAddCredentialWithResponse Start a ceremony to add another passkey to the signed-in account
	//
	// The authenticated counterpart to POST /api/auth/register/begin —
	// that one only ever works for a brand-new, credential-less
	// account; this is how an already-registered user adds a second
	// (or third...) passkey, e.g. a laptop and a security key (#355).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/credentials/begin (the `BeginAddCredential` operationId).
	BeginAddCredentialWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*BeginAddCredentialResponse, error)

	// FinishAddCredentialWithBodyWithResponse Complete an add-credential ceremony
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
	FinishAddCredentialWithBodyWithResponse(ctx context.Context, params *FinishAddCredentialParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishAddCredentialResponse, error)

	// FinishAddCredentialWithResponse Complete an add-credential ceremony
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
	FinishAddCredentialWithResponse(ctx context.Context, params *FinishAddCredentialParams, body FinishAddCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishAddCredentialResponse, error)

	// DeleteCredentialWithResponse Remove one of the signed-in user's own passkeys
	//
	// Refused on the account's last remaining passkey (#355) — this
	// app is WebAuthn-only with no password fallback, so deleting it
	// would lock the account out entirely.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/auth/credentials/{id} (the `DeleteCredential` operationId).
	DeleteCredentialWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*DeleteCredentialResponse, error)

	// BeginLoginWithBodyWithResponse Start a passkey login ceremony
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
	BeginLoginWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BeginLoginResponse, error)

	// BeginLoginWithResponse Start a passkey login ceremony
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
	BeginLoginWithResponse(ctx context.Context, body BeginLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*BeginLoginResponse, error)

	// FinishLoginWithBodyWithResponse Complete a passkey login ceremony
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
	FinishLoginWithBodyWithResponse(ctx context.Context, params *FinishLoginParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishLoginResponse, error)

	// FinishLoginWithResponse Complete a passkey login ceremony
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
	FinishLoginWithResponse(ctx context.Context, params *FinishLoginParams, body FinishLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishLoginResponse, error)

	// LogoutWithResponse End the current session
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/logout (the `Logout` operationId).
	LogoutWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*LogoutResponse, error)

	// BeginRegistrationWithBodyWithResponse Start a passkey registration ceremony
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
	BeginRegistrationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BeginRegistrationResponse, error)

	// BeginRegistrationWithResponse Start a passkey registration ceremony
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
	BeginRegistrationWithResponse(ctx context.Context, body BeginRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*BeginRegistrationResponse, error)

	// FinishRegistrationWithBodyWithResponse Complete a passkey registration ceremony
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
	FinishRegistrationWithBodyWithResponse(ctx context.Context, params *FinishRegistrationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishRegistrationResponse, error)

	// FinishRegistrationWithResponse Complete a passkey registration ceremony
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
	FinishRegistrationWithResponse(ctx context.Context, params *FinishRegistrationParams, body FinishRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishRegistrationResponse, error)

	// GetSessionWithResponse Who, if anyone, the current session cookie belongs to
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/auth/session (the `GetSession` operationId).
	GetSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSessionResponse, error)

	// GetDashboardWithResponse The aggregated view
	//
	// Returns the most recently fetched snapshot. The backend refreshes
	// this on its own schedule in the background; this endpoint never
	// blocks on a live call to either forge, so it answers instantly even
	// when a forge is slow or unreachable.
	//
	// Omitting `owner` returns the signed-in user's own dashboard. Passing
	// it returns that user's dashboard instead — allowed only when they've
	// shared it with the caller (or the caller is viewing their own
	// username), and refused with a 403 otherwise.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/dashboard (the `GetDashboard` operationId).
	GetDashboardWithResponse(ctx context.Context, params *GetDashboardParams, reqEditors ...RequestEditorFn) (*GetDashboardResponse, error)

	// RefreshDashboardWithResponse Trigger an immediate refresh of the signed-in user's own dashboard
	//
	// Blocks until an immediate, out-of-band refresh of the signed-in
	// user's own dashboard completes, then returns the resulting
	// snapshot — for retrying right away after a forge was only
	// briefly unreachable, instead of waiting for the next scheduled
	// background refresh.
	//
	// Doesn't support `owner` the way GET /api/dashboard does — only
	// ever the signed-in user's own dashboard, never a shared one, so
	// a user with shared access to someone else's dashboard can never
	// spend that owner's own forge rate-limit budget on their own
	// schedule.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/dashboard/refresh (the `RefreshDashboard` operationId).
	RefreshDashboardWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*RefreshDashboardResponse, error)

	// StreamDashboardWithResponse Server-Sent Events stream of the signed-in user's own dashboard
	//
	// Pushes a fresh snapshot every time this user's background
	// refresh produces one — most notably right after a verified
	// webhook delivery (see the webhooks tag) triggers an immediate
	// one, which is what turns that into a live update instead of
	// something only the next scheduled refresh picks up.
	//
	// Doesn't support `owner` the way GET /api/dashboard does — only
	// ever the signed-in user's own dashboard, never a shared one.
	// The frontend's own 30-second poll against GET /api/dashboard
	// keeps running unconditionally, connected or not — a browser or
	// proxy that can't hold an SSE connection open just never
	// benefits from this, rather than the dashboard going stale
	// silently.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/dashboard/stream (the `StreamDashboard` operationId).
	StreamDashboardWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*StreamDashboardResponse, error)

	// GetPullRequestChecksWithResponse List every job/check run against a pull request's head commit, on demand
	//
	// Fetched live on every call, never as part of GET /api/dashboard's
	// own snapshot — this is detail a pull request's row doesn't show
	// until a caller actually asks for it, and adding it to the eager
	// refresh loop would multiply that refresh's cost by every open
	// pull request for data most of them are never opened for.
	//
	// Falls back to the legacy combined commit-status API's own
	// per-check entries when the forge reports no Actions runs at all
	// for that commit (Actions disabled, or an instance too old to
	// expose them) — never an error for that case specifically, since
	// it's a normal, expected shape for an older or CI-less repo.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/pull-requests/checks (the `GetPullRequestChecks` operationId).
	GetPullRequestChecksWithResponse(ctx context.Context, params *GetPullRequestChecksParams, reqEditors ...RequestEditorFn) (*GetPullRequestChecksResponse, error)

	// PostPullRequestDependabotActionWithBodyWithResponse Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
	//
	// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
	// comment on the named pull request — Dependabot's own documented
	// comment-command interface, not a generic "post any comment"
	// endpoint; `action` is validated server-side to one of the two
	// values below and nothing else is ever sent. GitHub only —
	// Dependabot doesn't run on Forgejo.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
	PostPullRequestDependabotActionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PostPullRequestDependabotActionResponse, error)

	// PostPullRequestDependabotActionWithResponse Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
	//
	// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
	// comment on the named pull request — Dependabot's own documented
	// comment-command interface, not a generic "post any comment"
	// endpoint; `action` is validated server-side to one of the two
	// values below and nothing else is ever sent. GitHub only —
	// Dependabot doesn't run on Forgejo.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
	PostPullRequestDependabotActionWithResponse(ctx context.Context, body PostPullRequestDependabotActionJSONRequestBody, reqEditors ...RequestEditorFn) (*PostPullRequestDependabotActionResponse, error)

	// MergePullRequestWithBodyWithResponse Merge one pull request, on the signed-in user's behalf
	//
	// Merges the named pull request using its repo's own configured
	// default merge method — GitHub is asked to pick its own repo
	// default; Forgejo's API has no such default built in, so this
	// looks up the repo's configured default merge style first and
	// passes that explicitly. Neither takes a method override here;
	// picking one is out of scope for this endpoint.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
	MergePullRequestWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MergePullRequestResponse, error)

	// MergePullRequestWithResponse Merge one pull request, on the signed-in user's behalf
	//
	// Merges the named pull request using its repo's own configured
	// default merge method — GitHub is asked to pick its own repo
	// default; Forgejo's API has no such default built in, so this
	// looks up the repo's configured default merge style first and
	// passes that explicitly. Neither takes a method override here;
	// picking one is out of scope for this endpoint.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
	MergePullRequestWithResponse(ctx context.Context, body MergePullRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*MergePullRequestResponse, error)

	// PostPullRequestRenovateRebaseWithBodyWithResponse Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
	//
	// Adds the signed-in user's own configured Renovate rebase label
	// (Settings' "Renovate rebase label" field, defaulting to
	// Renovate's own `rebase` if left blank) to the named pull
	// request — Renovate's own documented rebase/retry trigger, which
	// is a label rather than a comment command. Works on both GitHub
	// and Forgejo, unlike the Dependabot actions, since Renovate runs
	// on both.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
	PostPullRequestRenovateRebaseWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PostPullRequestRenovateRebaseResponse, error)

	// PostPullRequestRenovateRebaseWithResponse Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
	//
	// Adds the signed-in user's own configured Renovate rebase label
	// (Settings' "Renovate rebase label" field, defaulting to
	// Renovate's own `rebase` if left blank) to the named pull
	// request — Renovate's own documented rebase/retry trigger, which
	// is a label rather than a comment command. Works on both GitHub
	// and Forgejo, unlike the Dependabot actions, since Renovate runs
	// on both.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
	PostPullRequestRenovateRebaseWithResponse(ctx context.Context, body PostPullRequestRenovateRebaseJSONRequestBody, reqEditors ...RequestEditorFn) (*PostPullRequestRenovateRebaseResponse, error)

	// UpdatePullRequestBranchWithBodyWithResponse Bring one pull request's branch up to date with its base, on the signed-in user's behalf
	//
	// Merges the named pull request's base branch into its own head
	// branch. GitHub can schedule this as a background job rather than
	// finishing it inline — a 202 here means exactly that, not a
	// failure; a follow-up refresh a moment later reflects the result.
	// Forgejo's equivalent is always synchronous, so it only ever
	// answers 204.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
	UpdatePullRequestBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdatePullRequestBranchResponse, error)

	// UpdatePullRequestBranchWithResponse Bring one pull request's branch up to date with its base, on the signed-in user's behalf
	//
	// Merges the named pull request's base branch into its own head
	// branch. GitHub can schedule this as a background job rather than
	// finishing it inline — a 202 here means exactly that, not a
	// failure; a follow-up refresh a moment later reflects the result.
	// Forgejo's equivalent is always synchronous, so it only ever
	// answers 204.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
	UpdatePullRequestBranchWithResponse(ctx context.Context, body UpdatePullRequestBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdatePullRequestBranchResponse, error)

	// DisableAutoUpdateBranchWithBodyWithResponse Turn off automatic branch updates for one tracked repo
	//
	// The reverse of POST /api/repos/auto-update-branch/enable.
	// Idempotent: disabling a repo that was never enabled is a no-op,
	// not an error.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
	DisableAutoUpdateBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DisableAutoUpdateBranchResponse, error)

	// DisableAutoUpdateBranchWithResponse Turn off automatic branch updates for one tracked repo
	//
	// The reverse of POST /api/repos/auto-update-branch/enable.
	// Idempotent: disabling a repo that was never enabled is a no-op,
	// not an error.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
	DisableAutoUpdateBranchWithResponse(ctx context.Context, body DisableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*DisableAutoUpdateBranchResponse, error)

	// EnableAutoUpdateBranchWithBodyWithResponse Turn on automatic branch updates for one tracked repo
	//
	// Any of this repo's pull requests the background refresh finds
	// behind its base branch gets updated automatically from then on
	// (#365), the same as clicking "Update branch" would — suppressed
	// for a bot-managed pull request unless bot-PR updates are
	// separately allowed. Idempotent: enabling an already-enabled repo
	// is a no-op, not an error.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
	EnableAutoUpdateBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EnableAutoUpdateBranchResponse, error)

	// EnableAutoUpdateBranchWithResponse Turn on automatic branch updates for one tracked repo
	//
	// Any of this repo's pull requests the background refresh finds
	// behind its base branch gets updated automatically from then on
	// (#365), the same as clicking "Update branch" would — suppressed
	// for a bot-managed pull request unless bot-PR updates are
	// separately allowed. Idempotent: enabling an already-enabled repo
	// is a no-op, not an error.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
	EnableAutoUpdateBranchWithResponse(ctx context.Context, body EnableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*EnableAutoUpdateBranchResponse, error)

	// IgnoreRepoWithBodyWithResponse Hide one tracked repo's pull requests and issues from the dashboard and Insights
	//
	// Reversible, not destructive (#363): the repo itself keeps
	// appearing in GET /api/dashboard's `repos` array with accurate
	// webhook-coverage status, and keeps being fetched and counted —
	// only its pullRequests/issues entries stop appearing there and on
	// Insights. Idempotent: ignoring an already-ignored repo is a
	// no-op, not an error.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
	IgnoreRepoWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*IgnoreRepoResponse, error)

	// IgnoreRepoWithResponse Hide one tracked repo's pull requests and issues from the dashboard and Insights
	//
	// Reversible, not destructive (#363): the repo itself keeps
	// appearing in GET /api/dashboard's `repos` array with accurate
	// webhook-coverage status, and keeps being fetched and counted —
	// only its pullRequests/issues entries stop appearing there and on
	// Insights. Idempotent: ignoring an already-ignored repo is a
	// no-op, not an error.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
	IgnoreRepoWithResponse(ctx context.Context, body IgnoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*IgnoreRepoResponse, error)

	// UnignoreRepoWithBodyWithResponse Stop ignoring one tracked repo
	//
	// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
	// repo that was never ignored is a no-op, not an error.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
	UnignoreRepoWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UnignoreRepoResponse, error)

	// UnignoreRepoWithResponse Stop ignoring one tracked repo
	//
	// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
	// repo that was never ignored is a no-op, not an error.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
	UnignoreRepoWithResponse(ctx context.Context, body UnignoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*UnignoreRepoResponse, error)

	// GetSettingsWithResponse The signed-in user's own forge configuration
	//
	// Tokens are never returned, before or after being saved — only
	// whether one is currently on file. There is no way to read a saved
	// token back out through this API.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/settings (the `GetSettings` operationId).
	GetSettingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSettingsResponse, error)

	// PutSettingsWithBodyWithResponse Save the signed-in user's forge configuration
	//
	// Replaces every field except a blank token, which means "keep
	// whatever's already saved" rather than "clear it" — the only
	// sensible behavior for a field the browser is never shown the
	// current value of. Saving triggers that user's dashboard to start
	// refreshing from the new configuration.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings (the `PutSettings` operationId).
	PutSettingsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PutSettingsResponse, error)

	// PutSettingsWithResponse Save the signed-in user's forge configuration
	//
	// Replaces every field except a blank token, which means "keep
	// whatever's already saved" rather than "clear it" — the only
	// sensible behavior for a field the browser is never shown the
	// current value of. Saving triggers that user's dashboard to start
	// refreshing from the new configuration.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings (the `PutSettings` operationId).
	PutSettingsWithResponse(ctx context.Context, body PutSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*PutSettingsResponse, error)

	// GetBotPrUpdatesSettingWithResponse Whether bot-managed pull request branches can be updated
	//
	// A lightweight, side-effect-free read of one field —
	// allowBotPrUpdates — for the dashboard page to check on every
	// load. Deliberately not GET /api/settings itself: that handler
	// also provisions webhook credentials on first call
	// (EnsureWebhookCredentials), which the dashboard visiting on a
	// user's behalf shouldn't trigger for someone who's never opened
	// Settings at all.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/settings/bot-pr-updates (the `GetBotPrUpdatesSetting` operationId).
	GetBotPrUpdatesSettingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetBotPrUpdatesSettingResponse, error)

	// GetFilterStateWithResponse The signed-in user's own saved dashboard/Insights filter state
	//
	// A lightweight read of one opaque blob (#353), the same
	// "deliberately not GET /api/settings itself" restraint
	// GET /api/settings/bot-pr-updates already uses, for the same
	// reason: this loads on every dashboard/Insights visit and
	// shouldn't provision webhook credentials as a side effect.
	// `{}` for a user who's never saved any filters yet, not a 404.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/settings/filter-state (the `GetFilterState` operationId).
	GetFilterStateWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetFilterStateResponse, error)

	// SetFilterStateWithBodyWithResponse Save the signed-in user's own dashboard/Insights filter state
	//
	// A dedicated, lightweight save separate from the main
	// PUT /api/settings — filters change on nearly every click, a
	// mismatch for that endpoint's "always a full form submit"
	// convention (#353). The body replaces the saved state entirely,
	// the same "always a full submit, just of a much smaller and
	// much more frequent thing" shape as the main settings PUT, not a
	// partial patch. Filters.js's own client-side code decides when
	// to call this: immediately for a discrete control, debounced
	// while the user is still typing in the free-text Title filter.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
	SetFilterStateWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetFilterStateResponse, error)

	// SetFilterStateWithResponse Save the signed-in user's own dashboard/Insights filter state
	//
	// A dedicated, lightweight save separate from the main
	// PUT /api/settings — filters change on nearly every click, a
	// mismatch for that endpoint's "always a full form submit"
	// convention (#353). The body replaces the saved state entirely,
	// the same "always a full submit, just of a much smaller and
	// much more frequent thing" shape as the main settings PUT, not a
	// partial patch. Filters.js's own client-side code decides when
	// to call this: immediately for a discrete control, debounced
	// while the user is still typing in the free-text Title filter.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
	SetFilterStateWithResponse(ctx context.Context, body SetFilterStateJSONRequestBody, reqEditors ...RequestEditorFn) (*SetFilterStateResponse, error)

	// GetThemeWithResponse The signed-in user's own saved theme preference
	//
	// A lightweight, side-effect-free read of one field — theme — for
	// every page to check on load (#352). The same "deliberately not
	// GET /api/settings itself" restraint GET /api/settings/bot-pr-updates
	// already uses, for the same reason: this loads on every page visit
	// and shouldn't provision webhook credentials as a side effect.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/settings/theme (the `GetTheme` operationId).
	GetThemeWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetThemeResponse, error)

	// SetThemeWithBodyWithResponse Save the signed-in user's own theme preference
	//
	// A dedicated, instant save (#352) — deliberately not routed
	// through the main PUT /api/settings, whose every other field is
	// a plain replace rather than a per-field merge: a request
	// carrying only theme through that handler would blank every
	// other saved setting. Picking a theme applies immediately, the
	// same "set once and forget" convention the ticket's own research
	// cites, rather than waiting on the rest of the settings form's
	// own Save button.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
	SetThemeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetThemeResponse, error)

	// SetThemeWithResponse Save the signed-in user's own theme preference
	//
	// A dedicated, instant save (#352) — deliberately not routed
	// through the main PUT /api/settings, whose every other field is
	// a plain replace rather than a per-field merge: a request
	// carrying only theme through that handler would blank every
	// other saved setting. Picking a theme applies immediately, the
	// same "set once and forget" convention the ticket's own research
	// cites, rather than waiting on the rest of the settings form's
	// own Save button.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
	SetThemeWithResponse(ctx context.Context, body SetThemeJSONRequestBody, reqEditors ...RequestEditorFn) (*SetThemeResponse, error)

	// GetSharingWithResponse The signed-in user's sharing relationships
	//
	// Both directions at once: who the signed-in user has shared their
	// own dashboard with, and whose dashboards have been shared with
	// them.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/sharing (the `GetSharing` operationId).
	GetSharingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSharingResponse, error)

	// UnshareWithWithResponse Stop sharing the signed-in user's dashboard with username
	//
	// Idempotent — removing a username that was never shared with changes nothing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/sharing/{username} (the `UnshareWith` operationId).
	UnshareWithWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*UnshareWithResponse, error)

	// ShareWithWithResponse Share the signed-in user's dashboard with username
	//
	// Idempotent — sharing with a username already on the list changes
	// nothing. username needs no tokens of their own configured to view
	// the shared dashboard; they see the owner's aggregated data exactly
	// as the owner sees it.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/sharing/{username} (the `ShareWith` operationId).
	ShareWithWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*ShareWithResponse, error)

	// ListAPITokensWithResponse List the signed-in user's own personal API tokens
	//
	// Metadata only, per APIToken's own description — a raw value never reappears once created.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/tokens (the `ListAPITokens` operationId).
	ListAPITokensWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListAPITokensResponse, error)

	// CreateAPITokenWithBodyWithResponse Generate a new personal API token
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
	CreateAPITokenWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAPITokenResponse, error)

	// CreateAPITokenWithResponse Generate a new personal API token
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
	CreateAPITokenWithResponse(ctx context.Context, body CreateAPITokenJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAPITokenResponse, error)

	// DeleteAPITokenWithResponse Revoke a personal API token
	//
	// Stops the token authenticating immediately. Idempotent — deleting
	// an id that's already gone (or never belonged to the signed-in
	// user) still answers 204, the same as any other delete-by-id
	// endpoint here (compare DELETE /api/sharing/{username}), so a
	// double-click or a retried request isn't an error.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/tokens/{id} (the `DeleteAPIToken` operationId).
	DeleteAPITokenWithResponse(ctx context.Context, id PathTokenID, reqEditors ...RequestEditorFn) (*DeleteAPITokenResponse, error)

	// GetVersionWithResponse The running server's own version
	//
	// The tag goreleaser stamped the running binary with ("dev" for a
	// build outside the release pipeline). Public and unauthenticated —
	// every page, including the pre-login screen, links to the matching
	// GitHub release from its footer so a person can tell what's
	// actually deployed without shelling into the box.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/version (the `GetVersion` operationId).
	GetVersionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetVersionResponse, error)

	// EnsureWebhookWithBodyWithResponse Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
	//
	// Creates a webhook on the named repo, pointed at the signed-in
	// user's own webhook URL and secret, subscribed to the events
	// docs/webhooks.md's manual steps recommend. If one already exists
	// there (found the same way GET /api/dashboard's own `hasWebhook`
	// does — matching by URL path, not object identity) it's brought
	// back to active with the right events instead of creating a
	// duplicate.
	//
	// One of a small number of endpoints in this API that write to a
	// forge rather than just reading from it — see also the
	// pull-requests tag.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
	EnsureWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EnsureWebhookResponse, error)

	// EnsureWebhookWithResponse Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
	//
	// Creates a webhook on the named repo, pointed at the signed-in
	// user's own webhook URL and secret, subscribed to the events
	// docs/webhooks.md's manual steps recommend. If one already exists
	// there (found the same way GET /api/dashboard's own `hasWebhook`
	// does — matching by URL path, not object identity) it's brought
	// back to active with the right events instead of creating a
	// duplicate.
	//
	// One of a small number of endpoints in this API that write to a
	// forge rather than just reading from it — see also the
	// pull-requests tag.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
	EnsureWebhookWithResponse(ctx context.Context, body EnsureWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*EnsureWebhookResponse, error)

	// ReceiveForgejoWebhookWithBodyWithResponse Receive a Forgejo repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's signature header is verified against that
	// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
	// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
	// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
	// delivery means the same thing here — something changed, refresh —
	// so nothing about the payload itself is parsed.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
	ReceiveForgejoWebhookWithBodyWithResponse(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReceiveForgejoWebhookResponse, error)

	// ReceiveForgejoWebhookWithResponse Receive a Forgejo repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's signature header is verified against that
	// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
	// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
	// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
	// delivery means the same thing here — something changed, refresh —
	// so nothing about the payload itself is parsed.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
	ReceiveForgejoWebhookWithResponse(ctx context.Context, webhookToken PathWebhookToken, body ReceiveForgejoWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*ReceiveForgejoWebhookResponse, error)

	// ReceiveGitHubWebhookWithBodyWithResponse Receive a GitHub repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's `X-Hub-Signature-256` header is verified against
	// that user's webhook secret. Every delivery GitHub can send for a
	// repository webhook (a new pull request, a closed issue, a status or
	// check-run update, even the `ping` event GitHub sends when the
	// webhook is first created) means the same thing here — something
	// changed, refresh — so nothing about the payload itself is parsed.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
	ReceiveGitHubWebhookWithBodyWithResponse(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReceiveGitHubWebhookResponse, error)

	// ReceiveGitHubWebhookWithResponse Receive a GitHub repository webhook delivery
	//
	// Triggers an immediate refresh for the user webhookToken identifies,
	// once the request's `X-Hub-Signature-256` header is verified against
	// that user's webhook secret. Every delivery GitHub can send for a
	// repository webhook (a new pull request, a closed issue, a status or
	// check-run update, even the `ping` event GitHub sends when the
	// webhook is first created) means the same thing here — something
	// changed, refresh — so nothing about the payload itself is parsed.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
	ReceiveGitHubWebhookWithResponse(ctx context.Context, webhookToken PathWebhookToken, body ReceiveGitHubWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*ReceiveGitHubWebhookResponse, error)

	// HealthWithResponse Liveness
	//
	// Answers 200 once the server has started. Doesn't reflect whether
	// either forge is reachable — see forges[].reachable on the dashboard
	// response for that.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /healthz (the `Health` operationId).
	HealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*HealthResponse, error)
}

type ListUsersResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *[]AdminUser
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListUsersResponse) GetJSON200() *[]AdminUser {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListUsersResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListUsersResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListUsersResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListUsersResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListUsersResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListUsersResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteUserResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r DeleteUserResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteUserResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteUserResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteUserResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r DeleteUserResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteUserResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteUserResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteUserResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RevokeUserResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r RevokeUserResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RevokeUserResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RevokeUserResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RevokeUserResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r RevokeUserResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RevokeUserResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RevokeUserResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RevokeUserResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListCredentialsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *[]Credential
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListCredentialsResponse) GetJSON200() *[]Credential {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListCredentialsResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListCredentialsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListCredentialsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListCredentialsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListCredentialsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BeginAddCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebAuthnCeremonyOptions
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BeginAddCredentialResponse) GetJSON200() *WebAuthnCeremonyOptions {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BeginAddCredentialResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r BeginAddCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BeginAddCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BeginAddCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BeginAddCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type FinishAddCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *Credential
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r FinishAddCredentialResponse) GetJSON201() *Credential {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r FinishAddCredentialResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r FinishAddCredentialResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r FinishAddCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r FinishAddCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r FinishAddCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r FinishAddCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r DeleteCredentialResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteCredentialResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r DeleteCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BeginLoginResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebAuthnCeremonyOptions
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BeginLoginResponse) GetJSON200() *WebAuthnCeremonyOptions {
	return r.JSON200
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r BeginLoginResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r BeginLoginResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BeginLoginResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BeginLoginResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BeginLoginResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type FinishLoginResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionUser
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r FinishLoginResponse) GetJSON200() *SessionUser {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r FinishLoginResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetBody returns the raw response body bytes
func (r FinishLoginResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r FinishLoginResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r FinishLoginResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r FinishLoginResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type LogoutResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r LogoutResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r LogoutResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r LogoutResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r LogoutResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BeginRegistrationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebAuthnCeremonyOptions
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BeginRegistrationResponse) GetJSON200() *WebAuthnCeremonyOptions {
	return r.JSON200
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r BeginRegistrationResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetBody returns the raw response body bytes
func (r BeginRegistrationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BeginRegistrationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BeginRegistrationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BeginRegistrationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type FinishRegistrationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionUser
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r FinishRegistrationResponse) GetJSON200() *SessionUser {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r FinishRegistrationResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetBody returns the raw response body bytes
func (r FinishRegistrationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r FinishRegistrationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r FinishRegistrationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r FinishRegistrationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSessionResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionUser
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSessionResponse) GetJSON200() *SessionUser {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSessionResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetSessionResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSessionResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSessionResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSessionResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetDashboardResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Dashboard
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetDashboardResponse) GetJSON200() *Dashboard {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetDashboardResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetDashboardResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetDashboardResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetDashboardResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetDashboardResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetDashboardResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetDashboardResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RefreshDashboardResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Dashboard
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RefreshDashboardResponse) GetJSON200() *Dashboard {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RefreshDashboardResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RefreshDashboardResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r RefreshDashboardResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RefreshDashboardResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RefreshDashboardResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RefreshDashboardResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type StreamDashboardResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r StreamDashboardResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r StreamDashboardResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r StreamDashboardResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r StreamDashboardResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r StreamDashboardResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r StreamDashboardResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetPullRequestChecksResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PullRequestChecksResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON200() *PullRequestChecksResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetPullRequestChecksResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetPullRequestChecksResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetPullRequestChecksResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetPullRequestChecksResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetPullRequestChecksResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PostPullRequestDependabotActionResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r PostPullRequestDependabotActionResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r PostPullRequestDependabotActionResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PostPullRequestDependabotActionResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PostPullRequestDependabotActionResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PostPullRequestDependabotActionResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MergePullRequestResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r MergePullRequestResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r MergePullRequestResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r MergePullRequestResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r MergePullRequestResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r MergePullRequestResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r MergePullRequestResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r MergePullRequestResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r MergePullRequestResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MergePullRequestResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MergePullRequestResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MergePullRequestResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PostPullRequestRenovateRebaseResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r PostPullRequestRenovateRebaseResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r PostPullRequestRenovateRebaseResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PostPullRequestRenovateRebaseResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PostPullRequestRenovateRebaseResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PostPullRequestRenovateRebaseResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdatePullRequestBranchResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdatePullRequestBranchResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdatePullRequestBranchResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdatePullRequestBranchResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdatePullRequestBranchResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdatePullRequestBranchResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DisableAutoUpdateBranchResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r DisableAutoUpdateBranchResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DisableAutoUpdateBranchResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r DisableAutoUpdateBranchResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DisableAutoUpdateBranchResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DisableAutoUpdateBranchResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DisableAutoUpdateBranchResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type EnableAutoUpdateBranchResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r EnableAutoUpdateBranchResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r EnableAutoUpdateBranchResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r EnableAutoUpdateBranchResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r EnableAutoUpdateBranchResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r EnableAutoUpdateBranchResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r EnableAutoUpdateBranchResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type IgnoreRepoResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r IgnoreRepoResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r IgnoreRepoResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r IgnoreRepoResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r IgnoreRepoResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r IgnoreRepoResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r IgnoreRepoResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UnignoreRepoResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UnignoreRepoResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UnignoreRepoResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r UnignoreRepoResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UnignoreRepoResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UnignoreRepoResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UnignoreRepoResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSettingsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SettingsResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSettingsResponse) GetJSON200() *SettingsResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSettingsResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetSettingsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSettingsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSettingsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSettingsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PutSettingsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SettingsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PutSettingsResponse) GetJSON200() *SettingsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PutSettingsResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PutSettingsResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r PutSettingsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PutSettingsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PutSettingsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PutSettingsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBotPrUpdatesSettingResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BotPrUpdatesResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBotPrUpdatesSettingResponse) GetJSON200() *BotPrUpdatesResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBotPrUpdatesSettingResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetBotPrUpdatesSettingResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBotPrUpdatesSettingResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBotPrUpdatesSettingResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBotPrUpdatesSettingResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetFilterStateResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *FilterState
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetFilterStateResponse) GetJSON200() *FilterState {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetFilterStateResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetFilterStateResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetFilterStateResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetFilterStateResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetFilterStateResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetFilterStateResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SetFilterStateResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SetFilterStateResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r SetFilterStateResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetFilterStateResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetFilterStateResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetFilterStateResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetThemeResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ThemeResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetThemeResponse) GetJSON200() *ThemeResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetThemeResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetThemeResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetThemeResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetThemeResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetThemeResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetThemeResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ThemeResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SetThemeResponse) GetJSON200() *ThemeResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SetThemeResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SetThemeResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r SetThemeResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetThemeResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetThemeResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetThemeResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSharingResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SharingResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSharingResponse) GetJSON200() *SharingResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSharingResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetSharingResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSharingResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSharingResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSharingResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UnshareWithResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UnshareWithResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r UnshareWithResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UnshareWithResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UnshareWithResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UnshareWithResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ShareWithResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ShareWithResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ShareWithResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ShareWithResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ShareWithResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ShareWithResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ShareWithResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ShareWithResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAPITokensResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *[]APIToken
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAPITokensResponse) GetJSON200() *[]APIToken {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAPITokensResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListAPITokensResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAPITokensResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAPITokensResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAPITokensResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateAPITokenResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *APITokenCreateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateAPITokenResponse) GetJSON201() *APITokenCreateResponse {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateAPITokenResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateAPITokenResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r CreateAPITokenResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateAPITokenResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateAPITokenResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateAPITokenResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteAPITokenResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteAPITokenResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r DeleteAPITokenResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteAPITokenResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteAPITokenResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteAPITokenResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetVersionResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Version
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetVersionResponse) GetJSON200() *Version {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetVersionResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetVersionResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetVersionResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetVersionResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type EnsureWebhookResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r EnsureWebhookResponse) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r EnsureWebhookResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r EnsureWebhookResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r EnsureWebhookResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r EnsureWebhookResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r EnsureWebhookResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r EnsureWebhookResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r EnsureWebhookResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r EnsureWebhookResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r EnsureWebhookResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReceiveForgejoWebhookResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ReceiveForgejoWebhookResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ReceiveForgejoWebhookResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ReceiveForgejoWebhookResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReceiveForgejoWebhookResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReceiveForgejoWebhookResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReceiveForgejoWebhookResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReceiveGitHubWebhookResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ReceiveGitHubWebhookResponse) GetJSON401() *Error {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ReceiveGitHubWebhookResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ReceiveGitHubWebhookResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReceiveGitHubWebhookResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReceiveGitHubWebhookResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReceiveGitHubWebhookResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type HealthResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Health
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r HealthResponse) GetJSON200() *Health {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r HealthResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r HealthResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r HealthResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r HealthResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListUsersWithResponse List every registered user
//
// Never includes a passkey or a forge credential — a username,
// display name, admin flag and registration timestamp per user.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/admin/users (the `ListUsers` operationId).
func (c *ClientWithResponses) ListUsersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListUsersResponse, error) {
	rsp, err := c.ListUsers(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListUsersResponse(rsp)
}

// DeleteUserWithResponse Remove a user outright
//
// Deletes the account, every session, every registered passkey, and
// every saved forge credential — irreversible. Their username
// becomes available for a fresh registration.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/admin/users/{username} (the `DeleteUser` operationId).
func (c *ClientWithResponses) DeleteUserWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*DeleteUserResponse, error) {
	rsp, err := c.DeleteUser(ctx, username, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteUserResponse(rsp)
}

// RevokeUserWithResponse Revoke a user's passkeys and sessions
//
// Deletes every session and every registered passkey for this user —
// they're signed out everywhere immediately, and have to register a
// new passkey from scratch to get back in. Their saved forge
// credentials are untouched; use the delete endpoint to remove the
// account itself.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/admin/users/{username}/revoke (the `RevokeUser` operationId).
func (c *ClientWithResponses) RevokeUserWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*RevokeUserResponse, error) {
	rsp, err := c.RevokeUser(ctx, username, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRevokeUserResponse(rsp)
}

// ListCredentialsWithResponse List the signed-in user's own passkeys
//
// Every passkey on the account (#355), oldest first — not just the
// one used to establish the current session.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/auth/credentials (the `ListCredentials` operationId).
func (c *ClientWithResponses) ListCredentialsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListCredentialsResponse, error) {
	rsp, err := c.ListCredentials(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListCredentialsResponse(rsp)
}

// BeginAddCredentialWithResponse Start a ceremony to add another passkey to the signed-in account
//
// The authenticated counterpart to POST /api/auth/register/begin —
// that one only ever works for a brand-new, credential-less
// account; this is how an already-registered user adds a second
// (or third...) passkey, e.g. a laptop and a security key (#355).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/credentials/begin (the `BeginAddCredential` operationId).
func (c *ClientWithResponses) BeginAddCredentialWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*BeginAddCredentialResponse, error) {
	rsp, err := c.BeginAddCredential(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBeginAddCredentialResponse(rsp)
}

// FinishAddCredentialWithBodyWithResponse Complete an add-credential ceremony
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
func (c *ClientWithResponses) FinishAddCredentialWithBodyWithResponse(ctx context.Context, params *FinishAddCredentialParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishAddCredentialResponse, error) {
	rsp, err := c.FinishAddCredentialWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishAddCredentialResponse(rsp)
}

// FinishAddCredentialWithResponse Complete an add-credential ceremony
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/credentials/finish (the `FinishAddCredential` operationId).
func (c *ClientWithResponses) FinishAddCredentialWithResponse(ctx context.Context, params *FinishAddCredentialParams, body FinishAddCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishAddCredentialResponse, error) {
	rsp, err := c.FinishAddCredential(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishAddCredentialResponse(rsp)
}

// DeleteCredentialWithResponse Remove one of the signed-in user's own passkeys
//
// Refused on the account's last remaining passkey (#355) — this
// app is WebAuthn-only with no password fallback, so deleting it
// would lock the account out entirely.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/auth/credentials/{id} (the `DeleteCredential` operationId).
func (c *ClientWithResponses) DeleteCredentialWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*DeleteCredentialResponse, error) {
	rsp, err := c.DeleteCredential(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteCredentialResponse(rsp)
}

// BeginLoginWithBodyWithResponse Start a passkey login ceremony
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
func (c *ClientWithResponses) BeginLoginWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BeginLoginResponse, error) {
	rsp, err := c.BeginLoginWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBeginLoginResponse(rsp)
}

// BeginLoginWithResponse Start a passkey login ceremony
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/login/begin (the `BeginLogin` operationId).
func (c *ClientWithResponses) BeginLoginWithResponse(ctx context.Context, body BeginLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*BeginLoginResponse, error) {
	rsp, err := c.BeginLogin(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBeginLoginResponse(rsp)
}

// FinishLoginWithBodyWithResponse Complete a passkey login ceremony
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
func (c *ClientWithResponses) FinishLoginWithBodyWithResponse(ctx context.Context, params *FinishLoginParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishLoginResponse, error) {
	rsp, err := c.FinishLoginWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishLoginResponse(rsp)
}

// FinishLoginWithResponse Complete a passkey login ceremony
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/login/finish (the `FinishLogin` operationId).
func (c *ClientWithResponses) FinishLoginWithResponse(ctx context.Context, params *FinishLoginParams, body FinishLoginJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishLoginResponse, error) {
	rsp, err := c.FinishLogin(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishLoginResponse(rsp)
}

// LogoutWithResponse End the current session
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/logout (the `Logout` operationId).
func (c *ClientWithResponses) LogoutWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*LogoutResponse, error) {
	rsp, err := c.Logout(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseLogoutResponse(rsp)
}

// BeginRegistrationWithBodyWithResponse Start a passkey registration ceremony
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
func (c *ClientWithResponses) BeginRegistrationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BeginRegistrationResponse, error) {
	rsp, err := c.BeginRegistrationWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBeginRegistrationResponse(rsp)
}

// BeginRegistrationWithResponse Start a passkey registration ceremony
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/register/begin (the `BeginRegistration` operationId).
func (c *ClientWithResponses) BeginRegistrationWithResponse(ctx context.Context, body BeginRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*BeginRegistrationResponse, error) {
	rsp, err := c.BeginRegistration(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBeginRegistrationResponse(rsp)
}

// FinishRegistrationWithBodyWithResponse Complete a passkey registration ceremony
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
func (c *ClientWithResponses) FinishRegistrationWithBodyWithResponse(ctx context.Context, params *FinishRegistrationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*FinishRegistrationResponse, error) {
	rsp, err := c.FinishRegistrationWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishRegistrationResponse(rsp)
}

// FinishRegistrationWithResponse Complete a passkey registration ceremony
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/auth/register/finish (the `FinishRegistration` operationId).
func (c *ClientWithResponses) FinishRegistrationWithResponse(ctx context.Context, params *FinishRegistrationParams, body FinishRegistrationJSONRequestBody, reqEditors ...RequestEditorFn) (*FinishRegistrationResponse, error) {
	rsp, err := c.FinishRegistration(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseFinishRegistrationResponse(rsp)
}

// GetSessionWithResponse Who, if anyone, the current session cookie belongs to
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/auth/session (the `GetSession` operationId).
func (c *ClientWithResponses) GetSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSessionResponse, error) {
	rsp, err := c.GetSession(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSessionResponse(rsp)
}

// GetDashboardWithResponse The aggregated view
//
// Returns the most recently fetched snapshot. The backend refreshes
// this on its own schedule in the background; this endpoint never
// blocks on a live call to either forge, so it answers instantly even
// when a forge is slow or unreachable.
//
// Omitting `owner` returns the signed-in user's own dashboard. Passing
// it returns that user's dashboard instead — allowed only when they've
// shared it with the caller (or the caller is viewing their own
// username), and refused with a 403 otherwise.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/dashboard (the `GetDashboard` operationId).
func (c *ClientWithResponses) GetDashboardWithResponse(ctx context.Context, params *GetDashboardParams, reqEditors ...RequestEditorFn) (*GetDashboardResponse, error) {
	rsp, err := c.GetDashboard(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetDashboardResponse(rsp)
}

// RefreshDashboardWithResponse Trigger an immediate refresh of the signed-in user's own dashboard
//
// Blocks until an immediate, out-of-band refresh of the signed-in
// user's own dashboard completes, then returns the resulting
// snapshot — for retrying right away after a forge was only
// briefly unreachable, instead of waiting for the next scheduled
// background refresh.
//
// Doesn't support `owner` the way GET /api/dashboard does — only
// ever the signed-in user's own dashboard, never a shared one, so
// a user with shared access to someone else's dashboard can never
// spend that owner's own forge rate-limit budget on their own
// schedule.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/dashboard/refresh (the `RefreshDashboard` operationId).
func (c *ClientWithResponses) RefreshDashboardWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*RefreshDashboardResponse, error) {
	rsp, err := c.RefreshDashboard(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRefreshDashboardResponse(rsp)
}

// StreamDashboardWithResponse Server-Sent Events stream of the signed-in user's own dashboard
//
// Pushes a fresh snapshot every time this user's background
// refresh produces one — most notably right after a verified
// webhook delivery (see the webhooks tag) triggers an immediate
// one, which is what turns that into a live update instead of
// something only the next scheduled refresh picks up.
//
// Doesn't support `owner` the way GET /api/dashboard does — only
// ever the signed-in user's own dashboard, never a shared one.
// The frontend's own 30-second poll against GET /api/dashboard
// keeps running unconditionally, connected or not — a browser or
// proxy that can't hold an SSE connection open just never
// benefits from this, rather than the dashboard going stale
// silently.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/dashboard/stream (the `StreamDashboard` operationId).
func (c *ClientWithResponses) StreamDashboardWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*StreamDashboardResponse, error) {
	rsp, err := c.StreamDashboard(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseStreamDashboardResponse(rsp)
}

// GetPullRequestChecksWithResponse List every job/check run against a pull request's head commit, on demand
//
// Fetched live on every call, never as part of GET /api/dashboard's
// own snapshot — this is detail a pull request's row doesn't show
// until a caller actually asks for it, and adding it to the eager
// refresh loop would multiply that refresh's cost by every open
// pull request for data most of them are never opened for.
//
// Falls back to the legacy combined commit-status API's own
// per-check entries when the forge reports no Actions runs at all
// for that commit (Actions disabled, or an instance too old to
// expose them) — never an error for that case specifically, since
// it's a normal, expected shape for an older or CI-less repo.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/pull-requests/checks (the `GetPullRequestChecks` operationId).
func (c *ClientWithResponses) GetPullRequestChecksWithResponse(ctx context.Context, params *GetPullRequestChecksParams, reqEditors ...RequestEditorFn) (*GetPullRequestChecksResponse, error) {
	rsp, err := c.GetPullRequestChecks(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetPullRequestChecksResponse(rsp)
}

// PostPullRequestDependabotActionWithBodyWithResponse Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
//
// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
// comment on the named pull request — Dependabot's own documented
// comment-command interface, not a generic "post any comment"
// endpoint; `action` is validated server-side to one of the two
// values below and nothing else is ever sent. GitHub only —
// Dependabot doesn't run on Forgejo.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
func (c *ClientWithResponses) PostPullRequestDependabotActionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PostPullRequestDependabotActionResponse, error) {
	rsp, err := c.PostPullRequestDependabotActionWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePostPullRequestDependabotActionResponse(rsp)
}

// PostPullRequestDependabotActionWithResponse Post one of Dependabot's own documented PR-comment commands on a pull request, on the signed-in user's behalf
//
// Posts exactly `@dependabot rebase` or `@dependabot recreate` as a
// comment on the named pull request — Dependabot's own documented
// comment-command interface, not a generic "post any comment"
// endpoint; `action` is validated server-side to one of the two
// values below and nothing else is ever sent. GitHub only —
// Dependabot doesn't run on Forgejo.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/dependabot-action (the `PostPullRequestDependabotAction` operationId).
func (c *ClientWithResponses) PostPullRequestDependabotActionWithResponse(ctx context.Context, body PostPullRequestDependabotActionJSONRequestBody, reqEditors ...RequestEditorFn) (*PostPullRequestDependabotActionResponse, error) {
	rsp, err := c.PostPullRequestDependabotAction(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePostPullRequestDependabotActionResponse(rsp)
}

// MergePullRequestWithBodyWithResponse Merge one pull request, on the signed-in user's behalf
//
// Merges the named pull request using its repo's own configured
// default merge method — GitHub is asked to pick its own repo
// default; Forgejo's API has no such default built in, so this
// looks up the repo's configured default merge style first and
// passes that explicitly. Neither takes a method override here;
// picking one is out of scope for this endpoint.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
func (c *ClientWithResponses) MergePullRequestWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MergePullRequestResponse, error) {
	rsp, err := c.MergePullRequestWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMergePullRequestResponse(rsp)
}

// MergePullRequestWithResponse Merge one pull request, on the signed-in user's behalf
//
// Merges the named pull request using its repo's own configured
// default merge method — GitHub is asked to pick its own repo
// default; Forgejo's API has no such default built in, so this
// looks up the repo's configured default merge style first and
// passes that explicitly. Neither takes a method override here;
// picking one is out of scope for this endpoint.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/merge (the `MergePullRequest` operationId).
func (c *ClientWithResponses) MergePullRequestWithResponse(ctx context.Context, body MergePullRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*MergePullRequestResponse, error) {
	rsp, err := c.MergePullRequest(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMergePullRequestResponse(rsp)
}

// PostPullRequestRenovateRebaseWithBodyWithResponse Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
//
// Adds the signed-in user's own configured Renovate rebase label
// (Settings' "Renovate rebase label" field, defaulting to
// Renovate's own `rebase` if left blank) to the named pull
// request — Renovate's own documented rebase/retry trigger, which
// is a label rather than a comment command. Works on both GitHub
// and Forgejo, unlike the Dependabot actions, since Renovate runs
// on both.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
func (c *ClientWithResponses) PostPullRequestRenovateRebaseWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PostPullRequestRenovateRebaseResponse, error) {
	rsp, err := c.PostPullRequestRenovateRebaseWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePostPullRequestRenovateRebaseResponse(rsp)
}

// PostPullRequestRenovateRebaseWithResponse Trigger Renovate's own rebase/retry on a pull request, on the signed-in user's behalf
//
// Adds the signed-in user's own configured Renovate rebase label
// (Settings' "Renovate rebase label" field, defaulting to
// Renovate's own `rebase` if left blank) to the named pull
// request — Renovate's own documented rebase/retry trigger, which
// is a label rather than a comment command. Works on both GitHub
// and Forgejo, unlike the Dependabot actions, since Renovate runs
// on both.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/renovate-rebase (the `PostPullRequestRenovateRebase` operationId).
func (c *ClientWithResponses) PostPullRequestRenovateRebaseWithResponse(ctx context.Context, body PostPullRequestRenovateRebaseJSONRequestBody, reqEditors ...RequestEditorFn) (*PostPullRequestRenovateRebaseResponse, error) {
	rsp, err := c.PostPullRequestRenovateRebase(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePostPullRequestRenovateRebaseResponse(rsp)
}

// UpdatePullRequestBranchWithBodyWithResponse Bring one pull request's branch up to date with its base, on the signed-in user's behalf
//
// Merges the named pull request's base branch into its own head
// branch. GitHub can schedule this as a background job rather than
// finishing it inline — a 202 here means exactly that, not a
// failure; a follow-up refresh a moment later reflects the result.
// Forgejo's equivalent is always synchronous, so it only ever
// answers 204.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
func (c *ClientWithResponses) UpdatePullRequestBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdatePullRequestBranchResponse, error) {
	rsp, err := c.UpdatePullRequestBranchWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePullRequestBranchResponse(rsp)
}

// UpdatePullRequestBranchWithResponse Bring one pull request's branch up to date with its base, on the signed-in user's behalf
//
// Merges the named pull request's base branch into its own head
// branch. GitHub can schedule this as a background job rather than
// finishing it inline — a 202 here means exactly that, not a
// failure; a follow-up refresh a moment later reflects the result.
// Forgejo's equivalent is always synchronous, so it only ever
// answers 204.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/pull-requests/update-branch (the `UpdatePullRequestBranch` operationId).
func (c *ClientWithResponses) UpdatePullRequestBranchWithResponse(ctx context.Context, body UpdatePullRequestBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdatePullRequestBranchResponse, error) {
	rsp, err := c.UpdatePullRequestBranch(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePullRequestBranchResponse(rsp)
}

// DisableAutoUpdateBranchWithBodyWithResponse Turn off automatic branch updates for one tracked repo
//
// The reverse of POST /api/repos/auto-update-branch/enable.
// Idempotent: disabling a repo that was never enabled is a no-op,
// not an error.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
func (c *ClientWithResponses) DisableAutoUpdateBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DisableAutoUpdateBranchResponse, error) {
	rsp, err := c.DisableAutoUpdateBranchWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDisableAutoUpdateBranchResponse(rsp)
}

// DisableAutoUpdateBranchWithResponse Turn off automatic branch updates for one tracked repo
//
// The reverse of POST /api/repos/auto-update-branch/enable.
// Idempotent: disabling a repo that was never enabled is a no-op,
// not an error.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/auto-update-branch/disable (the `DisableAutoUpdateBranch` operationId).
func (c *ClientWithResponses) DisableAutoUpdateBranchWithResponse(ctx context.Context, body DisableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*DisableAutoUpdateBranchResponse, error) {
	rsp, err := c.DisableAutoUpdateBranch(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDisableAutoUpdateBranchResponse(rsp)
}

// EnableAutoUpdateBranchWithBodyWithResponse Turn on automatic branch updates for one tracked repo
//
// Any of this repo's pull requests the background refresh finds
// behind its base branch gets updated automatically from then on
// (#365), the same as clicking "Update branch" would — suppressed
// for a bot-managed pull request unless bot-PR updates are
// separately allowed. Idempotent: enabling an already-enabled repo
// is a no-op, not an error.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
func (c *ClientWithResponses) EnableAutoUpdateBranchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EnableAutoUpdateBranchResponse, error) {
	rsp, err := c.EnableAutoUpdateBranchWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEnableAutoUpdateBranchResponse(rsp)
}

// EnableAutoUpdateBranchWithResponse Turn on automatic branch updates for one tracked repo
//
// Any of this repo's pull requests the background refresh finds
// behind its base branch gets updated automatically from then on
// (#365), the same as clicking "Update branch" would — suppressed
// for a bot-managed pull request unless bot-PR updates are
// separately allowed. Idempotent: enabling an already-enabled repo
// is a no-op, not an error.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/auto-update-branch/enable (the `EnableAutoUpdateBranch` operationId).
func (c *ClientWithResponses) EnableAutoUpdateBranchWithResponse(ctx context.Context, body EnableAutoUpdateBranchJSONRequestBody, reqEditors ...RequestEditorFn) (*EnableAutoUpdateBranchResponse, error) {
	rsp, err := c.EnableAutoUpdateBranch(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEnableAutoUpdateBranchResponse(rsp)
}

// IgnoreRepoWithBodyWithResponse Hide one tracked repo's pull requests and issues from the dashboard and Insights
//
// Reversible, not destructive (#363): the repo itself keeps
// appearing in GET /api/dashboard's `repos` array with accurate
// webhook-coverage status, and keeps being fetched and counted —
// only its pullRequests/issues entries stop appearing there and on
// Insights. Idempotent: ignoring an already-ignored repo is a
// no-op, not an error.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
func (c *ClientWithResponses) IgnoreRepoWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*IgnoreRepoResponse, error) {
	rsp, err := c.IgnoreRepoWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseIgnoreRepoResponse(rsp)
}

// IgnoreRepoWithResponse Hide one tracked repo's pull requests and issues from the dashboard and Insights
//
// Reversible, not destructive (#363): the repo itself keeps
// appearing in GET /api/dashboard's `repos` array with accurate
// webhook-coverage status, and keeps being fetched and counted —
// only its pullRequests/issues entries stop appearing there and on
// Insights. Idempotent: ignoring an already-ignored repo is a
// no-op, not an error.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/ignore (the `IgnoreRepo` operationId).
func (c *ClientWithResponses) IgnoreRepoWithResponse(ctx context.Context, body IgnoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*IgnoreRepoResponse, error) {
	rsp, err := c.IgnoreRepo(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseIgnoreRepoResponse(rsp)
}

// UnignoreRepoWithBodyWithResponse Stop ignoring one tracked repo
//
// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
// repo that was never ignored is a no-op, not an error.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
func (c *ClientWithResponses) UnignoreRepoWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UnignoreRepoResponse, error) {
	rsp, err := c.UnignoreRepoWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUnignoreRepoResponse(rsp)
}

// UnignoreRepoWithResponse Stop ignoring one tracked repo
//
// The reverse of POST /api/repos/ignore. Idempotent: un-ignoring a
// repo that was never ignored is a no-op, not an error.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/repos/unignore (the `UnignoreRepo` operationId).
func (c *ClientWithResponses) UnignoreRepoWithResponse(ctx context.Context, body UnignoreRepoJSONRequestBody, reqEditors ...RequestEditorFn) (*UnignoreRepoResponse, error) {
	rsp, err := c.UnignoreRepo(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUnignoreRepoResponse(rsp)
}

// GetSettingsWithResponse The signed-in user's own forge configuration
//
// Tokens are never returned, before or after being saved — only
// whether one is currently on file. There is no way to read a saved
// token back out through this API.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/settings (the `GetSettings` operationId).
func (c *ClientWithResponses) GetSettingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSettingsResponse, error) {
	rsp, err := c.GetSettings(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSettingsResponse(rsp)
}

// PutSettingsWithBodyWithResponse Save the signed-in user's forge configuration
//
// Replaces every field except a blank token, which means "keep
// whatever's already saved" rather than "clear it" — the only
// sensible behavior for a field the browser is never shown the
// current value of. Saving triggers that user's dashboard to start
// refreshing from the new configuration.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings (the `PutSettings` operationId).
func (c *ClientWithResponses) PutSettingsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PutSettingsResponse, error) {
	rsp, err := c.PutSettingsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePutSettingsResponse(rsp)
}

// PutSettingsWithResponse Save the signed-in user's forge configuration
//
// Replaces every field except a blank token, which means "keep
// whatever's already saved" rather than "clear it" — the only
// sensible behavior for a field the browser is never shown the
// current value of. Saving triggers that user's dashboard to start
// refreshing from the new configuration.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings (the `PutSettings` operationId).
func (c *ClientWithResponses) PutSettingsWithResponse(ctx context.Context, body PutSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*PutSettingsResponse, error) {
	rsp, err := c.PutSettings(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePutSettingsResponse(rsp)
}

// GetBotPrUpdatesSettingWithResponse Whether bot-managed pull request branches can be updated
//
// A lightweight, side-effect-free read of one field —
// allowBotPrUpdates — for the dashboard page to check on every
// load. Deliberately not GET /api/settings itself: that handler
// also provisions webhook credentials on first call
// (EnsureWebhookCredentials), which the dashboard visiting on a
// user's behalf shouldn't trigger for someone who's never opened
// Settings at all.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/settings/bot-pr-updates (the `GetBotPrUpdatesSetting` operationId).
func (c *ClientWithResponses) GetBotPrUpdatesSettingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetBotPrUpdatesSettingResponse, error) {
	rsp, err := c.GetBotPrUpdatesSetting(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBotPrUpdatesSettingResponse(rsp)
}

// GetFilterStateWithResponse The signed-in user's own saved dashboard/Insights filter state
//
// A lightweight read of one opaque blob (#353), the same
// "deliberately not GET /api/settings itself" restraint
// GET /api/settings/bot-pr-updates already uses, for the same
// reason: this loads on every dashboard/Insights visit and
// shouldn't provision webhook credentials as a side effect.
// `{}` for a user who's never saved any filters yet, not a 404.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/settings/filter-state (the `GetFilterState` operationId).
func (c *ClientWithResponses) GetFilterStateWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetFilterStateResponse, error) {
	rsp, err := c.GetFilterState(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetFilterStateResponse(rsp)
}

// SetFilterStateWithBodyWithResponse Save the signed-in user's own dashboard/Insights filter state
//
// A dedicated, lightweight save separate from the main
// PUT /api/settings — filters change on nearly every click, a
// mismatch for that endpoint's "always a full form submit"
// convention (#353). The body replaces the saved state entirely,
// the same "always a full submit, just of a much smaller and
// much more frequent thing" shape as the main settings PUT, not a
// partial patch. Filters.js's own client-side code decides when
// to call this: immediately for a discrete control, debounced
// while the user is still typing in the free-text Title filter.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
func (c *ClientWithResponses) SetFilterStateWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetFilterStateResponse, error) {
	rsp, err := c.SetFilterStateWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetFilterStateResponse(rsp)
}

// SetFilterStateWithResponse Save the signed-in user's own dashboard/Insights filter state
//
// A dedicated, lightweight save separate from the main
// PUT /api/settings — filters change on nearly every click, a
// mismatch for that endpoint's "always a full form submit"
// convention (#353). The body replaces the saved state entirely,
// the same "always a full submit, just of a much smaller and
// much more frequent thing" shape as the main settings PUT, not a
// partial patch. Filters.js's own client-side code decides when
// to call this: immediately for a discrete control, debounced
// while the user is still typing in the free-text Title filter.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings/filter-state (the `SetFilterState` operationId).
func (c *ClientWithResponses) SetFilterStateWithResponse(ctx context.Context, body SetFilterStateJSONRequestBody, reqEditors ...RequestEditorFn) (*SetFilterStateResponse, error) {
	rsp, err := c.SetFilterState(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetFilterStateResponse(rsp)
}

// GetThemeWithResponse The signed-in user's own saved theme preference
//
// A lightweight, side-effect-free read of one field — theme — for
// every page to check on load (#352). The same "deliberately not
// GET /api/settings itself" restraint GET /api/settings/bot-pr-updates
// already uses, for the same reason: this loads on every page visit
// and shouldn't provision webhook credentials as a side effect.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/settings/theme (the `GetTheme` operationId).
func (c *ClientWithResponses) GetThemeWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetThemeResponse, error) {
	rsp, err := c.GetTheme(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetThemeResponse(rsp)
}

// SetThemeWithBodyWithResponse Save the signed-in user's own theme preference
//
// A dedicated, instant save (#352) — deliberately not routed
// through the main PUT /api/settings, whose every other field is
// a plain replace rather than a per-field merge: a request
// carrying only theme through that handler would blank every
// other saved setting. Picking a theme applies immediately, the
// same "set once and forget" convention the ticket's own research
// cites, rather than waiting on the rest of the settings form's
// own Save button.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
func (c *ClientWithResponses) SetThemeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetThemeResponse, error) {
	rsp, err := c.SetThemeWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetThemeResponse(rsp)
}

// SetThemeWithResponse Save the signed-in user's own theme preference
//
// A dedicated, instant save (#352) — deliberately not routed
// through the main PUT /api/settings, whose every other field is
// a plain replace rather than a per-field merge: a request
// carrying only theme through that handler would blank every
// other saved setting. Picking a theme applies immediately, the
// same "set once and forget" convention the ticket's own research
// cites, rather than waiting on the rest of the settings form's
// own Save button.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/settings/theme (the `SetTheme` operationId).
func (c *ClientWithResponses) SetThemeWithResponse(ctx context.Context, body SetThemeJSONRequestBody, reqEditors ...RequestEditorFn) (*SetThemeResponse, error) {
	rsp, err := c.SetTheme(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetThemeResponse(rsp)
}

// GetSharingWithResponse The signed-in user's sharing relationships
//
// Both directions at once: who the signed-in user has shared their
// own dashboard with, and whose dashboards have been shared with
// them.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/sharing (the `GetSharing` operationId).
func (c *ClientWithResponses) GetSharingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSharingResponse, error) {
	rsp, err := c.GetSharing(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSharingResponse(rsp)
}

// UnshareWithWithResponse Stop sharing the signed-in user's dashboard with username
//
// Idempotent — removing a username that was never shared with changes nothing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/sharing/{username} (the `UnshareWith` operationId).
func (c *ClientWithResponses) UnshareWithWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*UnshareWithResponse, error) {
	rsp, err := c.UnshareWith(ctx, username, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUnshareWithResponse(rsp)
}

// ShareWithWithResponse Share the signed-in user's dashboard with username
//
// Idempotent — sharing with a username already on the list changes
// nothing. username needs no tokens of their own configured to view
// the shared dashboard; they see the owner's aggregated data exactly
// as the owner sees it.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/sharing/{username} (the `ShareWith` operationId).
func (c *ClientWithResponses) ShareWithWithResponse(ctx context.Context, username PathUsername, reqEditors ...RequestEditorFn) (*ShareWithResponse, error) {
	rsp, err := c.ShareWith(ctx, username, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseShareWithResponse(rsp)
}

// ListAPITokensWithResponse List the signed-in user's own personal API tokens
//
// Metadata only, per APIToken's own description — a raw value never reappears once created.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/tokens (the `ListAPITokens` operationId).
func (c *ClientWithResponses) ListAPITokensWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListAPITokensResponse, error) {
	rsp, err := c.ListAPITokens(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAPITokensResponse(rsp)
}

// CreateAPITokenWithBodyWithResponse Generate a new personal API token
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
func (c *ClientWithResponses) CreateAPITokenWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAPITokenResponse, error) {
	rsp, err := c.CreateAPITokenWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAPITokenResponse(rsp)
}

// CreateAPITokenWithResponse Generate a new personal API token
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/tokens (the `CreateAPIToken` operationId).
func (c *ClientWithResponses) CreateAPITokenWithResponse(ctx context.Context, body CreateAPITokenJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAPITokenResponse, error) {
	rsp, err := c.CreateAPIToken(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAPITokenResponse(rsp)
}

// DeleteAPITokenWithResponse Revoke a personal API token
//
// Stops the token authenticating immediately. Idempotent — deleting
// an id that's already gone (or never belonged to the signed-in
// user) still answers 204, the same as any other delete-by-id
// endpoint here (compare DELETE /api/sharing/{username}), so a
// double-click or a retried request isn't an error.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/tokens/{id} (the `DeleteAPIToken` operationId).
func (c *ClientWithResponses) DeleteAPITokenWithResponse(ctx context.Context, id PathTokenID, reqEditors ...RequestEditorFn) (*DeleteAPITokenResponse, error) {
	rsp, err := c.DeleteAPIToken(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteAPITokenResponse(rsp)
}

// GetVersionWithResponse The running server's own version
//
// The tag goreleaser stamped the running binary with ("dev" for a
// build outside the release pipeline). Public and unauthenticated —
// every page, including the pre-login screen, links to the matching
// GitHub release from its footer so a person can tell what's
// actually deployed without shelling into the box.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/version (the `GetVersion` operationId).
func (c *ClientWithResponses) GetVersionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetVersionResponse, error) {
	rsp, err := c.GetVersion(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetVersionResponse(rsp)
}

// EnsureWebhookWithBodyWithResponse Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
//
// Creates a webhook on the named repo, pointed at the signed-in
// user's own webhook URL and secret, subscribed to the events
// docs/webhooks.md's manual steps recommend. If one already exists
// there (found the same way GET /api/dashboard's own `hasWebhook`
// does — matching by URL path, not object identity) it's brought
// back to active with the right events instead of creating a
// duplicate.
//
// One of a small number of endpoints in this API that write to a
// forge rather than just reading from it — see also the
// pull-requests tag.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
func (c *ClientWithResponses) EnsureWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EnsureWebhookResponse, error) {
	rsp, err := c.EnsureWebhookWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEnsureWebhookResponse(rsp)
}

// EnsureWebhookWithResponse Create or fix up a webhook on one tracked repo, on the signed-in user's behalf
//
// Creates a webhook on the named repo, pointed at the signed-in
// user's own webhook URL and secret, subscribed to the events
// docs/webhooks.md's manual steps recommend. If one already exists
// there (found the same way GET /api/dashboard's own `hasWebhook`
// does — matching by URL path, not object identity) it's brought
// back to active with the right events instead of creating a
// duplicate.
//
// One of a small number of endpoints in this API that write to a
// forge rather than just reading from it — see also the
// pull-requests tag.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/ensure (the `EnsureWebhook` operationId).
func (c *ClientWithResponses) EnsureWebhookWithResponse(ctx context.Context, body EnsureWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*EnsureWebhookResponse, error) {
	rsp, err := c.EnsureWebhook(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEnsureWebhookResponse(rsp)
}

// ReceiveForgejoWebhookWithBodyWithResponse Receive a Forgejo repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's signature header is verified against that
// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
// delivery means the same thing here — something changed, refresh —
// so nothing about the payload itself is parsed.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
func (c *ClientWithResponses) ReceiveForgejoWebhookWithBodyWithResponse(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReceiveForgejoWebhookResponse, error) {
	rsp, err := c.ReceiveForgejoWebhookWithBody(ctx, webhookToken, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReceiveForgejoWebhookResponse(rsp)
}

// ReceiveForgejoWebhookWithResponse Receive a Forgejo repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's signature header is verified against that
// user's webhook secret — `X-Forgejo-Signature` on a webhook set up
// with the "Forgejo" type, `X-Gitea-Signature` on one set up with the
// legacy "Gitea" type; both carry the same raw hex HMAC-SHA256. Every
// delivery means the same thing here — something changed, refresh —
// so nothing about the payload itself is parsed.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/forgejo/{webhookToken} (the `ReceiveForgejoWebhook` operationId).
func (c *ClientWithResponses) ReceiveForgejoWebhookWithResponse(ctx context.Context, webhookToken PathWebhookToken, body ReceiveForgejoWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*ReceiveForgejoWebhookResponse, error) {
	rsp, err := c.ReceiveForgejoWebhook(ctx, webhookToken, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReceiveForgejoWebhookResponse(rsp)
}

// ReceiveGitHubWebhookWithBodyWithResponse Receive a GitHub repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's `X-Hub-Signature-256` header is verified against
// that user's webhook secret. Every delivery GitHub can send for a
// repository webhook (a new pull request, a closed issue, a status or
// check-run update, even the `ping` event GitHub sends when the
// webhook is first created) means the same thing here — something
// changed, refresh — so nothing about the payload itself is parsed.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
func (c *ClientWithResponses) ReceiveGitHubWebhookWithBodyWithResponse(ctx context.Context, webhookToken PathWebhookToken, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReceiveGitHubWebhookResponse, error) {
	rsp, err := c.ReceiveGitHubWebhookWithBody(ctx, webhookToken, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReceiveGitHubWebhookResponse(rsp)
}

// ReceiveGitHubWebhookWithResponse Receive a GitHub repository webhook delivery
//
// Triggers an immediate refresh for the user webhookToken identifies,
// once the request's `X-Hub-Signature-256` header is verified against
// that user's webhook secret. Every delivery GitHub can send for a
// repository webhook (a new pull request, a closed issue, a status or
// check-run update, even the `ping` event GitHub sends when the
// webhook is first created) means the same thing here — something
// changed, refresh — so nothing about the payload itself is parsed.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/webhooks/github/{webhookToken} (the `ReceiveGitHubWebhook` operationId).
func (c *ClientWithResponses) ReceiveGitHubWebhookWithResponse(ctx context.Context, webhookToken PathWebhookToken, body ReceiveGitHubWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*ReceiveGitHubWebhookResponse, error) {
	rsp, err := c.ReceiveGitHubWebhook(ctx, webhookToken, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReceiveGitHubWebhookResponse(rsp)
}

// HealthWithResponse Liveness
//
// Answers 200 once the server has started. Doesn't reflect whether
// either forge is reachable — see forges[].reachable on the dashboard
// response for that.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /healthz (the `Health` operationId).
func (c *ClientWithResponses) HealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*HealthResponse, error) {
	rsp, err := c.Health(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseHealthResponse(rsp)
}

// ParseListUsersResponse parses an HTTP response from a ListUsersWithResponse call
func ParseListUsersResponse(rsp *http.Response) (*ListUsersResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListUsersResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest []AdminUser
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseDeleteUserResponse parses an HTTP response from a DeleteUserWithResponse call
func ParseDeleteUserResponse(rsp *http.Response) (*DeleteUserResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteUserResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseRevokeUserResponse parses an HTTP response from a RevokeUserWithResponse call
func ParseRevokeUserResponse(rsp *http.Response) (*RevokeUserResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RevokeUserResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseListCredentialsResponse parses an HTTP response from a ListCredentialsWithResponse call
func ParseListCredentialsResponse(rsp *http.Response) (*ListCredentialsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListCredentialsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest []Credential
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseBeginAddCredentialResponse parses an HTTP response from a BeginAddCredentialWithResponse call
func ParseBeginAddCredentialResponse(rsp *http.Response) (*BeginAddCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BeginAddCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebAuthnCeremonyOptions
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseFinishAddCredentialResponse parses an HTTP response from a FinishAddCredentialWithResponse call
func ParseFinishAddCredentialResponse(rsp *http.Response) (*FinishAddCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &FinishAddCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest Credential
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseDeleteCredentialResponse parses an HTTP response from a DeleteCredentialWithResponse call
func ParseDeleteCredentialResponse(rsp *http.Response) (*DeleteCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseBeginLoginResponse parses an HTTP response from a BeginLoginWithResponse call
func ParseBeginLoginResponse(rsp *http.Response) (*BeginLoginResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BeginLoginResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebAuthnCeremonyOptions
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseFinishLoginResponse parses an HTTP response from a FinishLoginWithResponse call
func ParseFinishLoginResponse(rsp *http.Response) (*FinishLoginResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &FinishLoginResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionUser
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	}

	return response, nil
}

// ParseLogoutResponse parses an HTTP response from a LogoutWithResponse call
func ParseLogoutResponse(rsp *http.Response) (*LogoutResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &LogoutResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseBeginRegistrationResponse parses an HTTP response from a BeginRegistrationWithResponse call
func ParseBeginRegistrationResponse(rsp *http.Response) (*BeginRegistrationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BeginRegistrationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebAuthnCeremonyOptions
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	}

	return response, nil
}

// ParseFinishRegistrationResponse parses an HTTP response from a FinishRegistrationWithResponse call
func ParseFinishRegistrationResponse(rsp *http.Response) (*FinishRegistrationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &FinishRegistrationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionUser
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	}

	return response, nil
}

// ParseGetSessionResponse parses an HTTP response from a GetSessionWithResponse call
func ParseGetSessionResponse(rsp *http.Response) (*GetSessionResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSessionResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionUser
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetDashboardResponse parses an HTTP response from a GetDashboardWithResponse call
func ParseGetDashboardResponse(rsp *http.Response) (*GetDashboardResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetDashboardResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Dashboard
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseRefreshDashboardResponse parses an HTTP response from a RefreshDashboardWithResponse call
func ParseRefreshDashboardResponse(rsp *http.Response) (*RefreshDashboardResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RefreshDashboardResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Dashboard
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseStreamDashboardResponse parses an HTTP response from a StreamDashboardWithResponse call
func ParseStreamDashboardResponse(rsp *http.Response) (*StreamDashboardResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &StreamDashboardResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetPullRequestChecksResponse parses an HTTP response from a GetPullRequestChecksWithResponse call
func ParseGetPullRequestChecksResponse(rsp *http.Response) (*GetPullRequestChecksResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetPullRequestChecksResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PullRequestChecksResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParsePostPullRequestDependabotActionResponse parses an HTTP response from a PostPullRequestDependabotActionWithResponse call
func ParsePostPullRequestDependabotActionResponse(rsp *http.Response) (*PostPullRequestDependabotActionResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PostPullRequestDependabotActionResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseMergePullRequestResponse parses an HTTP response from a MergePullRequestWithResponse call
func ParseMergePullRequestResponse(rsp *http.Response) (*MergePullRequestResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MergePullRequestResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParsePostPullRequestRenovateRebaseResponse parses an HTTP response from a PostPullRequestRenovateRebaseWithResponse call
func ParsePostPullRequestRenovateRebaseResponse(rsp *http.Response) (*PostPullRequestRenovateRebaseResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PostPullRequestRenovateRebaseResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdatePullRequestBranchResponse parses an HTTP response from a UpdatePullRequestBranchWithResponse call
func ParseUpdatePullRequestBranchResponse(rsp *http.Response) (*UpdatePullRequestBranchResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdatePullRequestBranchResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 202:
		break // No content-type

	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseDisableAutoUpdateBranchResponse parses an HTTP response from a DisableAutoUpdateBranchWithResponse call
func ParseDisableAutoUpdateBranchResponse(rsp *http.Response) (*DisableAutoUpdateBranchResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DisableAutoUpdateBranchResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseEnableAutoUpdateBranchResponse parses an HTTP response from a EnableAutoUpdateBranchWithResponse call
func ParseEnableAutoUpdateBranchResponse(rsp *http.Response) (*EnableAutoUpdateBranchResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &EnableAutoUpdateBranchResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseIgnoreRepoResponse parses an HTTP response from a IgnoreRepoWithResponse call
func ParseIgnoreRepoResponse(rsp *http.Response) (*IgnoreRepoResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &IgnoreRepoResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseUnignoreRepoResponse parses an HTTP response from a UnignoreRepoWithResponse call
func ParseUnignoreRepoResponse(rsp *http.Response) (*UnignoreRepoResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UnignoreRepoResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetSettingsResponse parses an HTTP response from a GetSettingsWithResponse call
func ParseGetSettingsResponse(rsp *http.Response) (*GetSettingsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSettingsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SettingsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParsePutSettingsResponse parses an HTTP response from a PutSettingsWithResponse call
func ParsePutSettingsResponse(rsp *http.Response) (*PutSettingsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PutSettingsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SettingsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetBotPrUpdatesSettingResponse parses an HTTP response from a GetBotPrUpdatesSettingWithResponse call
func ParseGetBotPrUpdatesSettingResponse(rsp *http.Response) (*GetBotPrUpdatesSettingResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBotPrUpdatesSettingResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BotPrUpdatesResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetFilterStateResponse parses an HTTP response from a GetFilterStateWithResponse call
func ParseGetFilterStateResponse(rsp *http.Response) (*GetFilterStateResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetFilterStateResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest FilterState
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseSetFilterStateResponse parses an HTTP response from a SetFilterStateWithResponse call
func ParseSetFilterStateResponse(rsp *http.Response) (*SetFilterStateResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetFilterStateResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetThemeResponse parses an HTTP response from a GetThemeWithResponse call
func ParseGetThemeResponse(rsp *http.Response) (*GetThemeResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetThemeResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ThemeResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseSetThemeResponse parses an HTTP response from a SetThemeWithResponse call
func ParseSetThemeResponse(rsp *http.Response) (*SetThemeResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetThemeResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ThemeResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetSharingResponse parses an HTTP response from a GetSharingWithResponse call
func ParseGetSharingResponse(rsp *http.Response) (*GetSharingResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSharingResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SharingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseUnshareWithResponse parses an HTTP response from a UnshareWithWithResponse call
func ParseUnshareWithResponse(rsp *http.Response) (*UnshareWithResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UnshareWithResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseShareWithResponse parses an HTTP response from a ShareWithWithResponse call
func ParseShareWithResponse(rsp *http.Response) (*ShareWithResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ShareWithResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseListAPITokensResponse parses an HTTP response from a ListAPITokensWithResponse call
func ParseListAPITokensResponse(rsp *http.Response) (*ListAPITokensResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAPITokensResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest []APIToken
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseCreateAPITokenResponse parses an HTTP response from a CreateAPITokenWithResponse call
func ParseCreateAPITokenResponse(rsp *http.Response) (*CreateAPITokenResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateAPITokenResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest APITokenCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseDeleteAPITokenResponse parses an HTTP response from a DeleteAPITokenWithResponse call
func ParseDeleteAPITokenResponse(rsp *http.Response) (*DeleteAPITokenResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteAPITokenResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetVersionResponse parses an HTTP response from a GetVersionWithResponse call
func ParseGetVersionResponse(rsp *http.Response) (*GetVersionResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetVersionResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Version
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseEnsureWebhookResponse parses an HTTP response from a EnsureWebhookWithResponse call
func ParseEnsureWebhookResponse(rsp *http.Response) (*EnsureWebhookResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &EnsureWebhookResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseReceiveForgejoWebhookResponse parses an HTTP response from a ReceiveForgejoWebhookWithResponse call
func ParseReceiveForgejoWebhookResponse(rsp *http.Response) (*ReceiveForgejoWebhookResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReceiveForgejoWebhookResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseReceiveGitHubWebhookResponse parses an HTTP response from a ReceiveGitHubWebhookWithResponse call
func ParseReceiveGitHubWebhookResponse(rsp *http.Response) (*ReceiveGitHubWebhookResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReceiveGitHubWebhookResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseHealthResponse parses an HTTP response from a HealthWithResponse call
func ParseHealthResponse(rsp *http.Response) (*HealthResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &HealthResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Health
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}
